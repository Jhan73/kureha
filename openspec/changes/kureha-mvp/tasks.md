# Tasks: Kureha MVP — Plataforma operativa clinica gobernada

> **Deviation from skill word-budget (530 words):** this change spans 14 specs, a
> ~2,600-line design (hexagonal backend with 8 modules, a 15-node LangGraph, a
> frontend SPA, and AWS infra), so a 530-word checklist would not actually
> decompose the work — it would just relabel the design's section headers.
> Completeness was prioritized over the word cap; concision (1-2 lines/task,
> no prose) was kept. Flagged for the orchestrator/user, not silently ignored.

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | 8,000–15,000+ across the full MVP (backend 8 hexagonal modules + 15-node graph + frontend SPA x2 surfaces + AWS infra + migrations + tests) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | 17 work units, see table below (PR 1 → PR 17, mostly sequential with 3 parallelizable clusters) |
| Delivery strategy | ask-on-risk |
| Chain strategy | pending — user must choose `stacked-to-main` vs `feature-branch-chain` before `sdd-apply` starts |

Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: pending
400-line budget risk: High

**Tests travel with their work unit** (work-unit-commits skill): each PR below includes the unit/integration tests for what it ships. Phase 17 in the checklist below is reserved for cross-module integration suites that require multiple already-merged modules (e.g. full-stack RLS sweep, end-to-end schedule→audit→calendar-sync).

### Suggested Work Units

| Unit | Goal | Phase(s) | Depends on | Parallelizable? |
|------|------|----------|------------|-----------------|
| PR 1 | Tooling: deps, docker-compose, Alembic, import-linter | 1 | — | No — everything depends on this |
| PR 2 | Core schema + RLS: tenants/sites/users/patients/availability/appointments/consent/audit | 2.1-2.4 | PR 1 | No |
| PR 3 | Platform schema + RLS: RBAC/staff/calendar/sessions/rate/checkpointer tables | 2.5-2.11 | PR 2 | No |
| PR 4 | Shared kernel + governance modules (consent/audit/scope/rbac) | 3 | PR 3 | No |
| PR 5 | Identity + session management (AuthPort, Supabase adapter, token lifecycle) | 4 | PR 4 | No |
| PR 6 | Access-control middleware + rate limiting | 5 | PR 5 | No |
| PR 7 | Tenancy + Scheduling module | 6-7 | PR 6 | Yes — parallel with PR 8, PR 9 |
| PR 8 | Staff module | 8 | PR 6 | Yes — parallel with PR 7, PR 9 |
| PR 9 | Calendar sync module | 9 | PR 6 | Yes — parallel with PR 7, PR 8 |
| PR 10 | Platform inbound FastAPI (routers, composition root, error envelope) | 10 | PR 7, PR 8, PR 9 | No |
| PR 11 | LangGraph core (state, nodes, edges, HITL, confirmation_gate) | 11 | PR 10 | No |
| PR 12 | LangGraph streaming + guardrails + Tony UX | 12 | PR 11 | No |
| PR 13 | Platform hardening finalization (WAF config, error taxonomy wiring) | 13 | PR 12 | No |
| PR 14 | Frontend — patient portal + embedded chat | 14 | PR 12 | Yes — parallel with PR 15 |
| PR 15 | Frontend — staff copilot dashboard | 15 | PR 12 | Yes — parallel with PR 14 |
| PR 16 | AWS deployment + local dev parity | 16 | PR 3 (schema stable) | Partially — IaC authoring can start early, final wiring waits on PR 13 |
| PR 17 | Cross-module integration test suites + docs/cleanup | 17-18 | PR 14, PR 15, PR 16 | No |

For **feature-branch-chain**: PR 1 targets the `kureha-mvp` tracker branch; PR 2 targets PR 1's branch; each subsequent PR targets its immediate predecessor, except PR 8/PR 9 which both target PR 7's branch (parallel siblings) and PR 15 which targets PR 14's branch — retarget to keep diffs clean if any child shows a prior PR's changes.

---

## Phase 1: Tooling & Repo Foundation

- [x] 1.1 `backend/pyproject.toml`: add fastapi, uvicorn, sqlalchemy[asyncio], asyncpg, psycopg[binary,pool], alembic, langgraph, langgraph-checkpoint-postgres, langchain, pydantic-settings, cachetools, boto3, google-auth, google-api-python-client, pyjwt, import-linter, pytest, pytest-asyncio, httpx. Ref §1.
- [x] 1.2 Configure `import-linter` contracts: platform→modules→governance→shared_kernel one-way; no cross-module business imports; no `platform/` import inside `modules/`. Ref §2.4.
- [x] 1.3 `frontend/package.json` + `next.config.ts`: add react-markdown, rehype-sanitize; set `output: 'export'`. Ref §2.5, §8.8.
- [x] 1.4 `infra/localstack/init/*.sh` (secrets, s3, sns, cloudwatch) + `infra/postgres/init/`. Ref §22.5.
- [x] 1.5 `docker-compose.yml` (localstack, postgres, api) + `.env.local`. Ref §22.3/22.4.
- [x] 1.6 `backend/Dockerfile` (prod) + `backend/Dockerfile.dev` (hot-reload). Ref §2.5.
- [x] 1.7 Set up Alembic in `backend/migrations/`, async engine wiring.

## Phase 2: Database Schema + RLS

- [x] 2.1 Migration: `tenants`, `sites`, `users`, `professionals`, `patients` (tenant-wide identity, `UNIQUE(tenant_id, document_number)`). Ref §4.1.
- [x] 2.2 Migration: `availability`, `appointments` (`EXCLUDE USING gist` anti double-booking). Ref §4.1.
- [x] 2.3 Migration: `consent_policies`, `consents` (tenant-scoped, one current per tenant). Ref §4.1, §11.
- [x] 2.4 Migration: `audit_logs` + append-only triggers + hash-chain trigger with `pg_advisory_xact_lock`. Ref §4.3.
- [x] 2.5 Migration: `action_permissions` (with `bulk_cancel_threshold`), `role_permissions`, `user_permissions`. Ref §4.4, §5.
- [x] 2.6 Migration: `staff_members`, `shifts` (`EXCLUDE gist` no-overlap). Ref §4.4, §6.
- [x] 2.7 Migration: `calendar_credentials`, `calendar_sync` (`idempotency_key`, `UNIQUE(tenant_id, idempotency_key)`). Ref §4.4, §7.6.
- [x] 2.8 Migration: `user_sessions`, `rate_counters`, `tenants.llm_daily_budget_tokens`. Ref §4.4, §17.4, §19.
- [x] 2.9 RLS policies: `ENABLE`+`FORCE` on every tenant table per §4.2 patterns (tenant+site+role for most; tenant-wide self-policy for `patients`/`consents`/`calendar_credentials`).
- [x] 2.10 `AsyncPostgresSaver.setup()` + RLS on `checkpoints`/`checkpoint_writes` via `split_part(thread_id,':',1)`. Ref §4.4.
- [x] 2.11 `rate_counters` cleanup job (pg_cron or scheduled Lambda, TTL 24h). Ref §4.4.

## Phase 3: Shared Kernel + Governance Modules

- [x] 3.1 `shared_kernel/`: `TenantContext`, `DomainError` hierarchy, `ClockPort`/`SystemClock`, `IdGeneratorPort`/`UuidGenerator`.
- [x] 3.2 `modules/governance/consent`: `Consent`/`ConsentPolicy` domain, `CheckConsent` use case, postgres adapter. Ref §11.
- [x] 3.3 `modules/governance/audit`: `AuditEntry` domain, `AuditLogPort`, postgres adapter honoring the §4.3 action catalog.
- [x] 3.4 `modules/governance/scope`: `ClinicalScopePolicy` inbound+outbound classifier interface. Ref §8.7.
- [x] 3.5 `modules/governance/rbac`: `Permission`/`PermissionPolicy`, `AuthorizationPort`, `AuthorizeAction`/`ListAllowedActions` use cases (precedence: user override > role > deny-default), `PermissionService` adapter with **request-scoped memo only, no cross-request cache**. Ref §5, §5.6.
- [x] 3.6 Seed `action_permissions`/`role_permissions` (migration or seed script) with a **PLACEHOLDER, dev-only** role→action matrix — discovered during PR8 verify that with no seed data, `AuthorizeAction` denies every action by construction against a real Postgres (unit tests mask this via `_FakeAuthorizationPort`). The real per-tenant matrix content is business input pending per §16 ("input de negocio pendiente") — this placeholder unblocks local/dev/integration testing only; **must be replaced with the sign-off'd matrix before any non-dev environment**.

## Phase 4: Identity & Session Management

- [x] 4.1 `modules/identity`: `AuthPort` protocol (`verify_password`, `verify_federated`, `start_password_reset`), `AuthnResult` VO. Ref §17.1.
- [x] 4.2 `SupabaseAuthAdapter` implementing `AuthPort` (email/password, Google federated, password reset). Ref §17.2.
- [x] 4.3 `use_cases/login.py`: resolve `users` row from `AuthnResult.subject`, mint access JWT (~10min) + opaque refresh into `user_sessions`. Ref §17.3/17.4.
- [x] 4.4 `use_cases/refresh_token.py`: validate+rotate refresh, 30s grace period, reuse-detection revokes chain, re-check live active status, re-resolve role. Ref §17.4.
- [x] 4.5 `use_cases/logout.py` + `revoke_session.py` (admin-revoke all sessions for a `user_id`).
- [x] 4.6 Email-verification/account-linking flow when Google email matches an existing password account (no silent auto-merge). Ref specs/user-authentication.

## Phase 5: Access Control Middleware & Rate Limiting

- [x] 5.1 FastAPI middleware: validate access JWT, resolve `users` row, enforce live active-status gate (`users.status` AND `staff_members.status`), emit `SET LOCAL app.*`. Ref §4.2.
- [x] 5.2 Deny+audit path when token valid but no mappable `users` row (never default to a role). Ref §4.2.
- [x] 5.3 Rate-limit middleware layer 3: auth mint/refresh via `rate_counters` UPSERT; chat token-bucket per-instance + LLM daily budget cap (`llm.budget_exceeded` audit). Ref §19.

## Phase 6: Tenancy Module

- [x] 6.1 `modules/tenancy`: `Tenant` domain + `TenantPolicy`, postgres adapter, lookup use cases consumed by other modules.

## Phase 7: Scheduling Module

- [x] 7.1 Domain: `Appointment`, `Availability`, `RiskPolicy` (reads `bulk_cancel_threshold`, detects professional-change). Ref §8.4.
- [x] 7.2 Driven ports: `scheduling_repository`, `availability_repository`.
- [x] 7.3 Use cases: `schedule/reschedule/cancel_appointment`, `send_reminder` — each starts with `authorize(ctx, action)`. Ref §5.3.
- [x] 7.4 Postgres adapters (RLS-scoped queries, `EXCLUDE gist` conflict handling).
- [x] 7.5 In-process TTL availability cache (`cachetools.TTLCache`, key `tenant:site:resource:date`, bounded `maxsize`). Ref §18.

## Phase 8: Staff Module

- [x] 8.1 Domain: `StaffMember`, `Shift`, `StaffPolicy` (no-overlap; deactivate never deletes). Ref §6.
- [x] 8.2 Use cases: `register_staff`, `deactivate_staff`, `create_shift`, `edit_shift` behind `AuthorizeAction` (`staff.*`/`shift.*`), audited.
- [x] 8.3 Postgres adapters for staff/shift repositories.
- [x] 8.4 Scheduling enforces the `staff-registry` MUST scenario "deactivated staff cannot be scheduled" via a driven `StaffStatusPort` defined in `modules/scheduling` (queried by `schedule_appointment`/`reschedule_appointment`), consumed the same way `tenancy`'s `GetTenant` lookup is meant to be consumed by other modules — no cross-module Python imports; concrete adapter wired at composition root (Phase 10).

## Phase 9: Calendar Sync Module

- [x] 9.1 `modules/calendar`: `CalendarEventMapping` domain, `CalendarSyncPort` (`upsert_event`/`delete_event`), `CredentialVaultPort`. Ref §7.1.
- [x] 9.2 `AesGcmVault` adapter (envelope AES-256-GCM, KEK from Secrets Manager via injected `endpoint_url`). Ref §7.4, §22.6.
- [x] 9.3 `GoogleCalendarAdapter`: idempotency-key derivation (`base32hex(appointment_id)` + `kureha` prefix), `409`=success handling, OAuth2 `state` CSRF check. Ref §7.3, §7.6.
- [x] 9.4 Use cases: `connect_patient_calendar` (email-mismatch handling), `sync_appointment_to_calendar` (post-commit, non-transactional, best-effort). Ref §7.2/7.3.
- [x] 9.5 Retry/reconciliation job for `pending`/`failed` sync with backoff + `attempts` cap. Ref §7.5.

## Phase 10: Platform Inbound — FastAPI

- [x] 10.1 Routers: web forms (schedule/reschedule/cancel/reminder), auth endpoints (login/refresh/logout), Calendar OAuth2 callback. **Must** wire the callback to `calendar` module's existing `generate_oauth_state`/`verify_oauth_state` (currently unit-tested but unwired, no call sites — flagged during PR9 verify) and pass `state` into `ConnectPatientCalendar.execute()`; add the missing `AuditAction.CALENDAR_OAUTH_CSRF_ATTEMPT` catalog entry (design.md requires it, never added) and audit rejections.
  **Done (this session):** `backend/app/main.py` (FastAPI app factory + lifespan), `backend/app/platform/inbound/api/routers/{auth,scheduling,calendar_oauth}.py`, `backend/app/platform/inbound/api/access_control/dependencies.py`. 9 real routes: `/auth/{login,refresh,logout}`, `/appointments/{schedule,{id}/reschedule,{id}/cancel,{id}/reminder}`, `/calendar/oauth/{authorize,callback}`. **Deviation from this task's literal wording, flagged not silently reconciled:** `ConnectPatientCalendar.execute()` has no `state` parameter and its own docstring says CSRF verification belongs upstream of the use case — the callback router verifies `state` via `generate_oauth_state`/`verify_oauth_state` BEFORE calling `execute()`, never passes `state` into it (see `calendar_oauth.py`'s own module docstring for the full reasoning). `AuditAction.CALENDAR_OAUTH_CSRF_ATTEMPT` added to `audit_entry.py` + `default_role_permissions`-adjacent `action_catalog.py` needed NO new entry (calendar:connect already cataloged). Also added (needed to make the callback work end-to-end, not previously built anywhere): `GoogleCalendarAdapter.exchange_authorization_code` (the actual `grant_type=authorization_code` leg — every prior method started from an already-issued refresh token) + `CalendarOAuthExchangeError` (`calendar/domain/errors.py`) + `ConsoleReminderChannel` (`platform/outbound/channel/console_channel.py`, the MVP `ReminderChannelPort` `reminder_channel.py`'s own docstring flagged as missing). Router-level tests (`backend/tests/platform/inbound/api/routers/`, 10 tests, real Postgres + real LocalStack, `raise_server_exceptions=False` TestClient) prove per-endpoint: (a) valid request succeeds end-to-end, (b) unauthenticated/unauthorized denied through the real middleware+RBAC chain, (c) the §21 envelope shape on a domain error. Nonce for the OAuth CSRF check stored via a short-lived HttpOnly+Secure cookie (`generate_oauth_state`'s own docstring suggested `user_sessions.metadata`, which doesn't exist as a column — flagged deviation, not silent).
- [x] 10.2 `composition_root.py`: wire all adapters into use cases across every module. **Must** use `app.db.runtime_engine` (not `app.db.engine`) for every request-scoped repository/query adapter — `engine` connects as the `app_user` superuser and unconditionally bypasses RLS; only `runtime_engine` (`app_runtime` role) enforces it. See `app/db.py`'s module docstring and `tests/rls/test_app_runtime_role.py`. **Must** also construct a fresh `PermissionService` (`modules/governance/rbac`) per request, never a singleton/`lru_cache`-wrapped `Depends()` — its request-scoped memo is only safe if a new instance is built per request (design.md §5.6); add a test asserting this at the composition-root level. See `PermissionService`'s module docstring. **Must** also resolve `SyncAppointmentToCalendar`'s dual-role RLS requirement (`calendar_credentials_self` needs `app.role='patient'`, `calendar_sync_staff` needs staff roles) via a mid-transaction `SET LOCAL app.role` re-scope, and wire `UnwiredStaffStatusAdapter`/`UnwiredAppointmentSnapshotAdapter` (tasks 8.4/9.x seams) to real repositories.
  **Closed this session** (finishing a prior partial session — see composition_root.py's own module docstring for the full "Session 1"/"Session 2" split): added `open_elevated_connection()` (the pre-auth `app.db.engine` counterpart to `open_runtime_connection()`, needed by `Login`/`RefreshToken`/the access-control middleware's live-actor resolution) and `build_authorize_action`/`build_access_token_issuer`/`build_access_token_verifier`/`build_runtime_session`/`build_login`/`build_refresh_token`/`build_logout`/`build_schedule_appointment`/`build_reschedule_appointment`/`build_cancel_appointment`/`build_send_reminder`/`build_connect_patient_calendar`/`build_google_calendar_adapter` — full use-case wiring for identity (`Login`/`RefreshToken`/`Logout`), scheduling (`ScheduleAppointment`/`RescheduleAppointment`/`CancelAppointment`/`SendReminder`), and calendar (`ConnectPatientCalendar`), all consumed for real by task 10.1's routers and exercised end-to-end by their tests (not just unit-tested with fakes). `bootstrap_rbac_catalog_and_grants()` is now actually WIRED into `app/main.py`'s lifespan (previously built but uncalled). Staff module's own use cases (`RegisterStaff`/`DeactivateStaff`/`CreateShift`/`EditShift`) are deliberately NOT wired here — task 10.1's own text only asks for web-forms/auth/calendar-callback routers, no staff endpoints; inventing staff routes beyond the literal task wording was avoided per the orchestrator's own instruction to not invent scope.
- [x] 10.4 Before wiring a real calendar-disconnect/revoke flow: fix `PostgresCalendarCredentialRepository.revoke()` — it currently only sets `revoked_at` and does NOT clear `encrypted_refresh_token`/`nonce`/`wrapped_dek`, contradicting design.md §7.3 ("revoked_at + borrado del token cifrado") and its own port docstring (flagged during PR9 verify, currently dormant — no use case calls `revoke()` yet).
- [x] 10.3 Central exception handler mapping domain/infra errors to the §21 envelope (`error_code`/`category`/`user_message`/`retryable`/`correlation_id`); unmapped exceptions fall back to generic `internal_error`/500.
  **Closed this session:** the handler module (`platform/inbound/api/errors.py`) was already complete from a prior session but never called by any app. `app/main.py`'s `create_app()` now calls `register_exception_handlers(app)`. Router tests assert the exact envelope shape on real domain errors (404/403/422) end-to-end. **Gotcha discovered and worked around, not a code bug:** Starlette's `TestClient` defaults to re-raising any exception handled at the `Exception`/500 layer instead of returning the JSONResponse — tests must construct `TestClient(app, raise_server_exceptions=False)` to actually inspect the envelope (see `tests/platform/inbound/api/routers/conftest.py`'s `client` fixture docstring).

## Phase 11: LangGraph Core

- [x] 11.1 `platform/inbound/graph/state.py`: `KurehaState` TypedDict per §8.1.
  **Closed (PR 11 batch 1):** `RequestContext`/`ProposedAction`/`ApprovalDecision`/`ActionOutcome` defined alongside `KurehaState` in `state.py` — `RequestContext` is deliberately NOT `TenantContext` (missing `patient_id`/`professional_id`), see the module's own docstring for the flagged rationale and `to_tenant_context()` conversion.
- [x] 11.2 Nodes: `triage`, `clinical_scope_validator` (inbound), `consent_gate` (staff/shift bypass), `resolve_toolset`, `scheduling_agent`, `reminders_agent`, `staff_agent`, `rbac_gate` (in-memory `allowed_actions` shortcut). Ref §8.2, §5.6.
  **Closed (PR 11 batch 1):** all 8 nodes built as factory functions (`make_*_node(deps) -> node`) in `platform/inbound/graph/nodes/`, wired to real use cases (`ListAllowedActions`, `AuthorizeAction`, `CheckConsent`) and real domain policies (`RiskPolicy`). No LLM adapter exists yet anywhere in this codebase — `triage`/`scheduling_agent`/`reminders_agent`/`staff_agent` consume new Protocol-only seams (`platform/inbound/graph/ports/{intent_classifier,scheduling_planner,reminder_planner,staff_planner}.py`), extending the same pattern `ClinicalScopePolicy` already established for `clinical_scope_validator`. Flagged gap in `consent_gate`'s own docstring: `request_ctx.patient_id` is unresolved for a `staff_copilot` actor scheduling ON BEHALF OF a different patient (denies defensively) — not fixed in this batch, needs a design decision. `rbac_gate`'s in-memory shortcut is unit-tested with a port that raises `AssertionError` if called, proving the second Postgres query is skipped.
- [x] 11.3 `confirmation_gate` node: `not_required`/`needed`/`affirmed` logic; **must** explicitly reset `proposed_action=None` on every exit branch. Ref §8.9.
  **Fixed (PR 11 batch 3, Part 0):** batch 2's "unclear" branch always returned `"needed"`, regardless of whether this was the action's first proposal (Caso B, correct) or a reply to an already-asked prompt (Caso C, where an ambiguous reply must decline per §8.9's "cambio de topico" trigger) -- an ambiguous turn-N+1 reply looped forever re-asking instead of declining. Fixed by capturing `was_awaiting_reply = state.get("confirmation") == "needed"` at the top of the node, BEFORE this invocation recomputes anything -- design.md's "confirmation es None al inicio de cada turno" describes what the node must freshly compute on its way OUT, not a ban on reading the incoming checkpointed value as an input signal. Two new tests prove both the non-regressed first-pass behavior and the new reply-pass decline.
  **Closed (PR 11 batch 2):** `make_confirmation_gate_node` in `platform/inbound/graph/nodes/confirmation_gate.py`, backed by a new Protocol-only seam `AffirmationClassifierPort` (`platform/inbound/graph/ports/affirmation_classifier.py`) returning a THREE-way verdict (`"affirmed"|"declined"|"unclear"`, not boolean) — a plain boolean cannot express design.md §8.9's asymmetry between Caso B (turn N's original request, never a reply to anything → `"needed"`) and Caso C (turn N+1's reply to an already-asked prompt, where anything short of a clear yes → declined); see the node's own docstring for the full rationale. Confirmation prompt text is written to `response_text` (the only outbound-text field `KurehaState` has) — flagged explicitly as the field batch 3's `respond`/`response_guard` (task 11.5) must read/pass through. The critical invariant (every exit branch explicitly returns `proposed_action`, `None` on decline) is proven by a dedicated test asserting the returned dict's keys, not just "doesn't raise".
  **Real adapter closed (PR 12 batch 1):** `AnthropicAffirmationClassifier` (`platform/inbound/graph/adapters/anthropic_affirmation_classifier.py`), fast/small tier, takes `(ctx, message, *, pending_action_summary)` so it judges the reply against the ACTUAL pending action's text; fails closed to `"unclear"` on any error (never wrongly resolves to `"affirmed"`). Wired into `chat.py`'s `get_graph_dependencies`. 5 new tests.
- [x] 11.4 `hitl_approval` node (`interrupt()`), `ApprovalDecision` resume, `RiskPolicy` threshold wiring. Ref §8.4.
  **Closed (PR 11 batch 2):** `make_hitl_approval_node` in `platform/inbound/graph/nodes/hitl_approval.py` uses LangGraph's real `interrupt()`/`Command(resume=...)`, tested via a throwaway single-node graph + `MemorySaver` (not a bare function-call test — `interrupt()`'s semantics are coupled to the graph runtime). Both approve and reject branches are audited (`AuditAction.HITL_APPROVE`/`HITL_REJECT`) via `AuditLogPort` injected through the closure (judged against, and deliberately diverging from, `calendar_oauth.py`'s `_audit_csrf_attempt` separate-connection precedent — see the node's own docstring for why that reasoning doesn't apply here). Threshold/`requires_hitl` gap closed with a new `ActionRiskPort`/`ActionRiskReader` (`governance/rbac/application/ports/driven/action_risk.py`, `.../adapters/outbound/rbac/action_risk_reader.py`), request-scoped, no cross-request cache (mirrors `PermissionService`'s own privilege-escalation rationale). `scheduling_agent.py` gains `build_scheduling_agent_node` — a composition-time helper resolving the live `bulk_cancel_threshold` for `appointment:cancel` before constructing the node — `make_scheduling_agent_node`'s own signature/branching logic is untouched. Full `build_graph()`/composition-root wiring that actually CALLS these helpers is still task 11.6 (batch 3), not done here.
- [x] 11.5 `persist_and_audit` (single tx), `calendar_sync` (post-commit), `response_guard` (outbound), `direct_respond`, `escalate_human`, `deny_action`, `respond` (+ suggestions §8.11.2).
  **Closed (PR 11 batch 3):** all 7 nodes built in `platform/inbound/graph/nodes/`. `persist_and_audit` dispatches via a static `ActionKey -> composition_root.build_*` table (added `build_register_staff`/`build_deactivate_staff`/`build_create_shift`/`build_edit_shift` -- no staff use case had ANY composition-root wiring before this batch); does not write its own audit entry (every use case already audits in its own transaction, ADR-3) and does NOT catch exceptions (no failure edge exists in design.md's diagram; propagates to the central `errors.py` handler like every other router). **Flagged, unresolved gap:** `state.audit_ref` stays `None` from this node -- none of the 8 dispatchable use cases' `execute()` returns the audit row id their internal `audit_log.record()` call produces. `calendar_sync` wires the real `SyncAppointmentToCalendar`; extended `AppointmentSyncSnapshot` (calendar's own port) with a new optional `site_id` field since no `calendar_sync` row exists yet for a FIRST sync to source it from. **Flagged, unresolved gap:** a `patient`-role actor (self-service) has no staff `base_role` to satisfy `calendar_sync`'s staff-only RLS policy -- reports `calendar_sync_status="failed"` without attempting the write rather than inventing an unauthorized role escalation (same posture as `consent_gate`'s own flagged gap). `response_guard`/`respond` together resolve a real structural tension in design.md's own edge diagram (response_guard runs BEFORE respond even on the operational persist_and_audit path, where nothing has set `response_text` yet) -- documented at length in both nodes' own docstrings: `response_guard` classifies `state.get("response_text") or ""` (vacuously safe when unset), `respond`'s fallback composition is template-based from structured fields only (never fresh LLM text), so no second guard pass is needed. `respond`'s suggestions use a new `SuggestionGeneratorPort` seam for TEXT only -- the RBAC-safety filter (never suggest an action outside `allowed_actions`) is enforced in plain code per this task's own explicit instruction. `direct_respond` uses a new `DirectResponsePort` seam (Tony's full system prompt is task 12.5, out of scope).
  **Post-verify CRITICAL fix:** a fresh-context `sdd-verify` pass after batch 3 found that NONE of the terminal nodes (`persist_and_audit`/`calendar_sync`/`response_guard`/`respond`/`deny_action`/`escalate_human`/`hitl_approval`) ever returned `proposed_action` in their state update -- since `KurehaState` has no `Annotated[..., reducer]` fields, LangGraph's default `LastValue` channel keeps the last checkpointed value forever, so `proposed_action` survived every completed turn and `route_from_start` misrouted the NEXT, unrelated turn straight into `confirmation_gate` (empirically reproduced: re-executing `persist_and_audit` against an already-booked slot raised `SlotUnavailableError`). Fixed in `respond.py` (the one node every path passes through before `END`): clears `proposed_action` to `None` on every exit EXCEPT `confirmation_gate`'s own `"needed"` prompt (which must survive for `route_from_start` to find on the reply turn); `confirmation` itself is deliberately left untouched (nothing downstream keys off a stale non-`"needed"` value, and forcing it to `None` would destroy the caller-visible record of what happened that turn). Covered by a new `respond.py` unit test plus a turn-N+2 extension of `test_build_graph.py`'s real-compiled-graph round trip, proving the checkpoint is clean and the following unrelated turn correctly re-enters `triage`.
- [x] 11.6 `route_from_start` conditional edge + full edge wiring per §8.3; `build_graph()` with `AsyncPostgresSaver`, `thread_id = "{tenant_id}:{user_id}:{client_random}"`.
  **Closed (PR 11 batch 3):** `platform/inbound/graph/build_graph.py`. All 17 nodes (8 batch 1 + 2 batch 2 + 7 this batch) wired into one `StateGraph(KurehaState)` per design.md §8.3's diagram verbatim, including `route_after_confirmation` (merges design.md's separate `confirmation_gate` branch + `route_by_risk` into one conditional-edge function, since LangGraph edges route to real node names only -- reads `ActionRiskPort` live, a SECOND read independent of `hitl_approval`'s own internal one, per this task's own instruction that a routing function cannot call a node's internals) and `route_after_persist` (live "patient has a connected calendar" check via `scoped_as_patient`, the same dual-role RLS mechanism `build_sync_appointment_to_calendar` already used). **Deliberate, documented construction-time choice:** `build_graph()` compiles a FRESH graph PER REQUEST (not once at app startup) -- see the module's own docstring for the full reasoning (connection ownership, not performance, is what forces this). Every LLM-shaped seam port defaults to a new `Unwired*` placeholder (`graph/adapters/unwired.py`, `governance/scope/adapters/outbound/unwired/`), following the EXACT precedent `UnwiredStaffStatusAdapter`/`UnwiredAppointmentSnapshotAdapter` already established -- no real LLM adapter exists anywhere in this codebase yet (tasks.md Phase 12's job). `AsyncPostgresSaver.setup()` is deliberately NOT called anywhere -- migration `043b5dd9768e` already ran the sync twin's `.setup()` + RLS. **Flagged, unresolved gap:** the checkpointer's own psycopg connection needs `app.tenant_id` set for its RLS policy to filter correctly -- `composition_root.open_checkpointer_connection(tenant_id)` (new this batch) is the one place that does this (session-scoped `SET`, not `SET LOCAL`), but only task 11.7's chat endpoint calls it; a future POOLED checkpointer connection would need a proper per-checkout GUC-reset mechanism this codebase does not have yet. Two real integration tests (`tests/platform/inbound/graph/test_build_graph.py`, real Postgres via `rls_conn`, fakes only for LLM seams) prove: (a) a low-risk web_form schedule routes end-to-end through `persist_and_audit` to `respond`, (b) the turn-N/turn-N+1 `confirmation_gate` round trip works through the REAL compiled graph (not the node in isolation).
- [x] 11.7 Chat endpoint: server-side `thread_id` ownership validation (assembled from token claims + client random). Ref §8.6.
  **Closed (PR 11 batch 3):** `platform/inbound/api/routers/chat.py`, `POST /chat`, mounted in `app/main.py`. `thread_id` assembled server-side from `get_tenant_context`/`get_live_actor` (both already resolved by `AccessControlMiddleware` from the caller's verified access token) + an optional client-supplied `client_random_uuid` (server-generated when absent) -- never trusts a client-supplied `tenant_id`/`user_id`. Non-streaming `graph.ainvoke()` only (SSE is task 12.1). **Deliberate, documented choice:** only `request_ctx`/`channel`/`channel_message` are passed as the invoke input -- a full `KurehaState` literal with an explicit `"proposed_action": None` would silently wipe a pending turn-N confirmation before `route_from_start` ever read it (LangGraph applies every key present in an invoke input as an unconditional overwrite, same as a node's own return dict). Router tests (`tests/platform/inbound/api/routers/test_chat.py`) prove (a) unauthenticated denied 401, (b) an authenticated request reaches the graph and surfaces the (expected, until Phase 12) `Unwired*` seam failure through the SAME central `errors.py` envelope every other router uses -- proving the endpoint's own auth/thread_id/graph-construction wiring is genuinely exercised, not merely importable.

## Phase 12: LangGraph Streaming + Guardrails + Tony UX

- [x] 12.1 `POST /chat/stream`: `StreamingResponse`, `graph.astream(stream_mode=[messages,updates,custom])` → SSE `token`/`status`/`done`/`error`. Ref §8.5.
  **Closed (PR 12 batch 3):** `platform/inbound/api/routers/chat.py`'s new `chat_stream()`/`_stream_turn()`. Real SSE wire format (`streaming/sse.py`'s `format_sse_event`), `status`/`token`/`done`/`error` events, `graph.astream(..., stream_mode=["messages","updates","custom"])` used verbatim. **Genuine bug found and fixed this batch:** `AccessControlMiddleware` (a `BaseHTTPMiddleware`) closes its request-scoped `conn` in a `finally` around `call_next()`, which for a `StreamingResponse` resolves BEFORE the generator body runs -- reproduced empirically (`ResourceClosedError`) -- fixed by having `_stream_turn` open its OWN dedicated connection via `build_runtime_session().begin(live_actor)`/`.end(conn, commit=...)`, scoped to the generator's own lifetime, not the middleware's `call_next()` return. **Honest, flagged scope boundary:** no LLM adapter in this codebase generates incrementally (`.with_structured_output(...).ainvoke()` is single-shot for every one of the 8 real Anthropic adapters) -- `token` events are built by re-segmenting the graph's FINAL `response_text` (observed via `respond`'s own `updates` chunk) through the sentence-boundary + streaming-guard pipeline (task 12.4) rather than genuinely concurrent-with-generation delivery; `status` events ARE genuinely live (task 12.2). 3 new router tests in `tests/platform/inbound/api/routers/test_chat.py`.
  **Rate-limiter/LLM-budget wiring also closed this batch (design.md §19, the gap PR 12 batch 2's `composition_root.py` docstring flagged: "`record_usage`... has NO caller anywhere in this codebase").** Reused the EXISTING, previously-unwired `ChatRateLimiter`/`LlmBudgetGuard`/`TokenBucketRegistry`/`PostgresRateCounterStore` (built in an earlier PR6 session, task 5.3b) -- no new rate-limiting primitive invented. `composition_root.build_chat_rate_limiter(conn)` wires a process-wide `TokenBucketRegistry` singleton (mirrors `_rotation_replay_cache`'s own precedent) with `LlmBudgetGuard`. New `composition_root.build_get_tenant(conn)` resolves `tenants.llm_daily_budget_tokens`. Both `chat()` and `chat_stream()` call a new shared `_enforce_rate_limit()` helper BEFORE the graph runs and `ChatRateLimiter.record_usage()` (new passthrough method) after, via a new `TokenUsageCallbackHandler` (`streaming/token_usage.py`) passed as `config={"callbacks":[...]}` to `graph.ainvoke()`/`graph.astream()`. **Scoped to `patient_chat` only** (spec `platform-hardening` is titled "Rate Limiting on Patient Chat"; no spec names an equivalent requirement for `staff_copilot` -- a deliberate, flagged scope boundary). **Defensive, not guessed:** whether `.with_structured_output(...).ainvoke()` genuinely surfaces `usage_metadata` through LangChain's callback dispatch is UNVERIFIED against a live Anthropic call in this environment -- `on_llm_end` defensively `getattr`s every field, a shape mismatch contributes `0` rather than crashing the turn. 5 new tests (`streaming/test_token_usage.py`) + 3 new `composition_root` tests + 1 new `ChatRateLimiter.record_usage` test.
- [x] 12.2 `get_stream_writer()` status events scoped to `allowed_actions` only (no tool-name leakage). Ref §8.5.
  **Adapter-half closed (PR 12 batch 1, not this task's own SSE scope):** the real `IntentClassifierPort` adapter `triage` needs (`AnthropicIntentClassifier`, `platform/inbound/graph/adapters/anthropic_intent_classifier.py`) is built and wired -- see `build_graph.py`'s `GraphDependencies.intent_classifier` via `chat.py`'s `get_graph_dependencies`.
  **SSE status-event half closed (PR 12 batch 3):** `platform/inbound/graph/streaming/status_writer.py`'s `emit_status(*, phase, label, action=None, allowed_actions=None)` -- the ONE place any node calls `get_stream_writer()`. Scoped to `allowed_actions`: an event tied to a specific `action` is suppressed unless that action is in the caller's own `allowed_actions` (spec `internal-staff-copilot`, "Streaming status shows only permitted tool activity"); `action=None` events (administrative/generic, e.g. "resolving the toolset") are always emitted. Safe outside a running graph (`get_stream_writer()` raises `RuntimeError` outside a LangGraph runnable context -- confirmed empirically -- caught and treated as a no-op, since every existing node test in this package calls the bare node function directly, never through a compiled graph). Wired into 5 nodes: `resolve_toolset`, `scheduling_agent`, `staff_agent`, `reminders_agent`, `calendar_sync` (design.md's own two named examples plus the other 3 specialist/toolset nodes). 6 new tests for `emit_status` itself + 5 new node-level tests (one per instrumented node) proving the right `phase`/`action`/`allowed_actions` are passed.
- [x] 12.3 Extend `clinical_scope_validator` inbound with injection/jailbreak + tenant/scope-leakage framing categories. Ref §8.7.
  **Closed (PR 12 batch 1):** the 4-category enum (`in_scope`/`clinical_diagnosis`/`prompt_injection`/`tenant_scope_leakage`) already existed since task 3.4 (`governance/scope/domain/scope_policy.py`) and `clinical_scope_validator.py`'s node consumption already correctly maps ANY non-`IN_SCOPE` category to `scope_ok=False` (verified, unchanged) -- what this batch closes is the REAL classifier: `AnthropicScopePolicy` (`governance/scope/adapters/outbound/anthropic/anthropic_scope_policy.py`), fast/small tier, a tightly-scoped system prompt (embeds `ctx.tenant_id` as the reference point `ClinicalScopePolicy`'s own docstring says a raw-text-only classifier lacks) + Pydantic structured output constrained to the exact `Literal` category strings. Implements BOTH `classify_inbound` (this task) AND `classify_outbound` (`response_guard`, task 11.5's already-built node, now real for the first time) in one adapter, since design.md §8.10 puts both on the same fast tier. Outbound short-circuits an empty chunk to `safe` without a network call (the operational-path gap `response_guard.py`'s own docstring flags). Fails closed (never silently `in_scope`/`safe`) on any classifier error. Wired into `chat.py`'s `get_graph_dependencies` (overridable in tests, same `app.dependency_overrides` pattern as `get_http_client`). 11 new tests, `tests/modules/governance/scope/adapters/test_anthropic_scope_policy.py`.
- [x] 12.4 `response_guard` sentence-boundary buffering (~80-token fallback), async per-chunk classification gating SSE `token` emission. Ref §8.7.
  **Closed (PR 12 batch 3):** `platform/inbound/graph/streaming/sentence_buffer.py`'s `SentenceBoundaryBuffer` implements design.md's exact algorithm (flush on `\n`/`.`/`?`/`!`, or a ~80-word fallback -- flagged, deliberate approximation: "tokens" means whitespace-separated words, since this codebase has no tokenizer dependency; a soft UX-latency fallback, not a security boundary). `platform/inbound/graph/streaming/response_guard_stream.py`'s `guard_sentence_units` classifies each unit via `ClinicalScopePolicy.classify_outbound`, scheduling classification as an `asyncio.Task` the instant a unit is ready and only awaiting/yielding it once the NEXT unit starts being pulled from the upstream iterator -- the overlap mechanism design.md describes. An unsafe unit raises `ResponseGuardStreamRefusal`, mapped to the SAME `clinical_scope_refused` §21 envelope via `errors.py`'s new `resolve_error()` (also newly public, reused by `/chat/stream`'s `error` SSE event since an exception raised inside an already-started `StreamingResponse` body cannot reach FastAPI's own registered exception handlers). Relationship to the existing (task 11.5) `nodes/response_guard.py`: ADDITIVE, not a replacement -- that node still runs unchanged as the graph's whole-text safety backstop before `respond`; this is the SECOND, transport-layer guard applied to how the already-approved text gets chunked/delivered over SSE (genuinely redundant classification of the same content today, honestly flagged, but the exact mechanism needed the moment generation becomes incremental). 14 new tests (9 `SentenceBoundaryBuffer` + 5 `guard_sentence_units`).
- [x] 12.5 `direct_respond` for `greeting`/`capability_query`/`small_talk` + Tony identity/limits system prompt. Ref §8.11.1/8.11.3.
  **Closed (PR 12 batch 2):** `AnthropicDirectResponse` (`platform/inbound/graph/adapters/anthropic_direct_response.py`), fast tier. Builds Tony's full system-prompt identity (§8.11.3: name, administrative role, explicit "never diagnoses/prescribes" clinical limit, friendly/direct/concise tone, replies in the user's own language, Markdown per §8.8) -- the PR 11 batch 3 closure note on `direct_respond.py` explicitly deferred this content to this task; now built. `capability_query` derives its capability list STRICTLY from the caller's own `allowed_actions` (§8.11.1's hard rule), reusing `ACTION_CATALOG`'s existing `key -> description` entries rather than a second hand-maintained table -- proven by a dedicated test that an action outside `allowed_actions` never reaches the prompt. Falls back to a canned reply (never raises) on an LLM failure -- a deliberate difference from the scheduling/staff/reminder planners (see 12.7's note): `direct_respond` has no side effect beyond a chat reply, so degrading gracefully is safe and reasonable here, unlike a planner feeding a real mutation. Wired into `chat.py`'s `get_graph_dependencies` via `composition_root.build_direct_response`. 5 new tests, `tests/platform/inbound/graph/adapters/test_anthropic_direct_response.py`.
- [x] 12.6 `respond`: up to 3 RBAC-safe proactive suggestions, truncation, `None` when unjustified. Ref §8.11.2.
  **Closed (PR 12 batch 2):** `AnthropicSuggestionGenerator` (`platform/inbound/graph/adapters/anthropic_suggestion_generator.py`), fast tier, generates candidate suggestion TEXT + an optional `.action` tag only. **Verified, not just assumed: `respond.py`'s own RBAC-safety filter (`c.action is None or c.action in allowed_actions`) and truncation-to-3 logic are UNCHANGED by this batch** -- the only edit to `respond.py` was adding `proposed_action_summary` to the `SuggestionContext` it builds (see below), the filter/truncation lines themselves were not touched, confirmed by re-reading the diff and by `test_respond.py`'s existing 8 tests still passing unmodified. `SuggestionContext` (`ports/suggestion_generator.py`) gained a new optional `proposed_action_summary` field, threaded from `respond.py`'s already-known `proposed_action.summary` -- design.md §8.11.2's own examples are contextual to the JUST-COMPLETED action ("¿Agregar un recordatorio para ESTA cita?"), which the pre-existing `intent`/`outcome_success` pair could not express on its own. Wired into `chat.py`'s `get_graph_dependencies` via `composition_root.build_suggestion_generator`. 5 new tests, `tests/platform/inbound/graph/adapters/test_anthropic_suggestion_generator.py`.
- [x] 12.7 Per-node LLM tier via env vars (never hardcoded). Ref §8.10.
  **Fully closed (PR 12 batch 2).** PR 12 batch 1 built `build_chat_model(tier, *, settings=None)` and wired 3/8 adapters through it; this batch adds the remaining 5 (`AnthropicSchedulingPlanner`/`AnthropicStaffPlanner` on the reasoner tier, `AnthropicReminderPlanner`/`AnthropicDirectResponse`/`AnthropicSuggestionGenerator` on the fast tier), all via the SAME factory -- every LLM-invoking node in design.md §8.10's table now resolves its model through one place, none hardcoded. `chat.py`'s `get_graph_dependencies` builds exactly TWO `ChatAnthropic` instances per request (one per tier) and shares each across the adapters that need that tier, rather than 8 separate HTTP clients.
  **Genuine, UNRESOLVED gap flagged prominently, NOT closed by this task or this batch (out of this task's own scope -- an architectural/graph-topology gap, not a "which tier" question):** neither `SchedulingPlannerPort`/`StaffPlannerPort`/`ReminderPlannerPort` implementation can invent a real database id (`patient_id`/`professional_id`/`availability_id`/`appointment_id`/`staff_member_id`/`shift_id`) from conversational text alone -- `SchedulingPlannerPort.plan()`'s own `ctx: TenantContext` parameter does not even carry the caller's OWN `patient_id`/`professional_id` (those live on `RequestContext`, dropped by `scheduling_agent.py`'s `to_tenant_context()` conversion before calling `plan()`), and no lookup-by-natural-language-reference tool/port exists anywhere in this graph for availability/appointment/staff/shift. `AnthropicSchedulingPlanner`'s own module docstring documents this at length (the canonical write-up; `AnthropicStaffPlanner`/`AnthropicReminderPlanner` cross-reference it). Practical consequence: `plan.kwargs` only ever contains fields the model actually extracted from the message (never a fabricated id, never a `None` placeholder) -- a real conversational schedule/reschedule/cancel/staff/shift/reminder request will almost always pass `rbac_gate`/`confirmation_gate` (those never inspect `payload`) and only fail at `persist_and_audit` with a clean, loud missing-kwarg `TypeError` (or `AppointmentNotFoundError("")` for reminders) once the user has already affirmed. Safe (no data corruption, no wrong-patient mutation) but NOT a good UX, and NOT production-ready for the conversational path -- `web_form`'s deterministic flow (real ids from a UI that already resolved them) remains the only production-ready path for these 6 actions today. Closing this for real needs either a new graph node/tool-call round trip that resolves natural-language references to real ids, or `SchedulingPlannerPort`/`StaffPlannerPort`/`ReminderPlannerPort` gaining an explicit "needs more info" return shape a clarification flow could route to -- neither invented here; flagged as the natural next step for whoever picks this up (likely a dedicated follow-up, not batch 3, since either option is graph-topology work, not just another adapter).

## Phase 13: Platform Hardening Finalization

- [x] 13.1 Error-taxonomy envelope wired into SSE `error` events (cross-ref 10.3).
  **Closed (PR 13):** already correctly built during PR 12 batch 3 (`chat.py`'s `_stream_turn` already wraps its entire generator body in `except Exception as exc: resolved = resolve_error(exc); yield format_sse_event("error", ...)`) -- this task was about proving the LAST uncovered path: an unmapped exception (not a rate-limit/guard-refusal, both already tested). New test `test_chat_stream_surfaces_an_unmapped_exception_as_a_non_leaky_internal_error_event` (`tests/platform/inbound/api/routers/test_chat.py`) makes `triage`'s `intent_classifier.classify()` raise a plain `RuntimeError` carrying a fake connection string mid-`graph.astream()`, asserts the SSE `error` event resolves to `errors.py`'s `internal_error`/`internal` fallback and that the raw message never leaks into `user_message` -- passed with ZERO code changes, confirming this was already correctly implemented, not a real gap.
- [x] 13.2 Cache-invariant checks: every cache key tenant-prefixed, cache never bypasses RLS/RBAC (code review checklist + assertions).
  **Closed (PR 13).** Full checklist of every in-process cache found in `backend/app/`: (1) `CachedAvailabilityRepository` (`modules/scheduling/adapters/outbound/cache/availability_cache.py`) -- already tenant-prefixed (`{tenant_id}:{site_id}:{professional_id}:{date}`); had a key-differs-by-dimension test but no test proving the actual data-leakage invariant, closed with new `test_two_tenants_never_share_a_cache_entry_for_the_same_site_resource_and_date` (asserts distinct tenant-tagged values, not just call counts). (2) `TTLRotationReplayCache` (`modules/identity/adapters/outbound/tokens/ttl_rotation_replay_cache.py`) -- keyed by `old_refresh_token_hash` only, no tenant prefix; judged a JUSTIFIED exception (a cryptographic hash of a per-user secret is practically unique across tenants already) and documented as one line in the module docstring, plus new `test_a_hash_never_returns_a_different_hashs_cached_pair` proving no cross-key leakage. (3) RBAC: `PermissionService` (`governance/rbac/adapters/outbound/rbac/permission_service.py`) memoizes ONLY within one request-scoped instance (already covered by the existing `test_build_permission_service_returns_a_fresh_instance_every_call` in `test_composition_root.py`, which proves `build_permission_service` is never `@lru_cache`d/singleton-wired) -- already correct, no new test forced. `ActionRiskReader` (`.../action_risk_reader.py`) caches nothing at all -- N/A. (4) `config.py`'s `get_settings()` `@lru_cache` -- tenant-independent static `Settings` singleton, justified exception, no invariant applies. (5) `anthropic_scope_policy.py`'s lazily-bound runnable -- per-instance LLM binding, not tenant data, justified. (6) `aes_gcm_vault.py`'s `_kek_cache` -- a data-encryption key cached per (freshly-constructed-per-call) `AesGcmVault` instance, not tenant/RLS/RBAC data, justified, already documented. (7) `llm.py` grep hit was a false positive (docstring prose only, no actual cache). Also reviewed `TokenBucketRegistry` (`platform/inbound/api/rate_limit/token_bucket.py`, not in the original grep list since it doesn't literally match "cache") -- keyed by `f"{tenant_id}:{patient_id}"` per its own docstring, out of this task's literal cache-grep scope and not an authorization/RLS decision cache (rate-limiter state, isolation is inherent to its key design, not a bypass risk) -- noted, not forced into scope. No genuine unfixed gap found across any cache.

## Phase 14: Frontend — Patient Portal & Embedded Chat

- [x] 14.1 Auth pages (email/password + "Sign in with Google"), access-token-in-memory strategy, refresh flow, route guards.
  **Closed (PR 14 batch A, first frontend implementation work in this change).** First real frontend code in this repo: `frontend/src/lib/api/{client,config,types}.ts` (fetch wrapper: `login`/`refresh`/`logout` + `createAuthorizedFetch` with single-flight refresh-then-retry-once on 401, deduping concurrent 401s into one shared refresh call, no infinite retry loop), `frontend/src/lib/auth/{auth-context,refresh-token-storage,require-auth}.tsx` (React context holding access token + user in-memory only, `<RequireAuth>` client-side route guard since `output: "export"` has no Next.js Middleware), `frontend/src/app/login/page.tsx` (tenant_id/email/password form + visibly-disabled "Sign in with Google" button, no fake federated flow), `frontend/src/app/portal/page.tsx` (placeholder post-login landing, first real `<RequireAuth>` consumer, logout button). shadcn/ui initialized for the first time in this repo (Vega preset per `frontend/AGENTS.md`) + `button`/`input`/`label`/`card`/`alert` components added; `@tabler/icons-react` installed (AGENTS.md mandates Tabler over shadcn's lucide-react default, only actually needed for the Google icon so far). **Storage decision, owned not defaulted:** access token in-memory only (React state, lost on reload by design); refresh token persisted to `localStorage` (`refresh-token-storage.ts`'s own docstring has the full reasoning) since the backend returns it as plain JSON, not an httpOnly cookie -- in-memory-only would force full re-login on every reload for a ~30-day credential; accepts the standard localStorage XSS tradeoff, mitigated by the backend's own rotation-with-reuse-detection (design.md §17.4 ADR-15). Added `@testing-library/jest-dom` + a `vitest.setup.ts` (registers RTL's `afterEach(cleanup)` globally, `vitest.config.ts` had no `test.globals`/setup file before this task and multiple test files now render more than once per suite). 24 new tests (9 client, 3 storage, 5 auth-context, 4 RequireAuth, 4 login page, 2 portal page) + the pre-existing root page test, 28/28 passing; `npm run lint` clean (0 errors -- fixed 2 real `eslint-plugin-react-hooks` errors surfaced during this task: a ref-read-during-render hazard in `auth-context.tsx`, refactored to plain `useState` instead of `useRef` for the refresh token mirror; a synchronous `setState`-in-effect in `require-auth.tsx`, refactored so the "already authenticated" branch is derived from `accessToken` at render time instead of mirrored into local state); `npm run build` succeeds (static export emits `out/{index,login,portal}.html`, verified by grepping the emitted HTML rather than a live browser -- no browser-automation tool available in this environment). **Flagged, not silently invented:** `LoginRequest.tenant_id` is a real product gap upstream of this task (backend doesn't resolve tenant from a subdomain) -- the login form exposes a plain visible "Clinic / Tenant ID" text field per the task's own explicit MVP-scope guidance. `npm audit` reports 15 pre-existing vulnerabilities (postcss/sharp, transitively via `next@16.2.10`) unrelated to any package added this task -- not fixed here (`npm audit fix --force` would bump `next` outside this task's scope). Root `frontend/src/app/page.tsx` (default Next.js template) deliberately left untouched -- removing scaffolding placeholders is tasks.md 17.5, not this task. 14.2 (self-service web forms), 14.3 (embedded chat), 14.4 (markdown rendering), 14.5 (confirmation-prompt UX) are separate, later batches -- not started here.
- [x] 14.2 Self-service web forms: schedule/reschedule/cancel/reminder views over deterministic API routes.
  **Closed (PR 14 batch B, same work unit as 14.1).** Four `<RequireAuth>`-wrapped web-form views under `frontend/src/app/portal/appointments/{schedule,reschedule,cancel,reminder}/page.tsx`, each a thin form over one existing backend route (`backend/app/platform/inbound/api/routers/scheduling.py`'s `POST /appointments/schedule|{id}/reschedule|{id}/cancel|{id}/reminder` -- no new backend endpoints, no divergent client-side business rules, per `patient-self-service-portal`'s "no channel-specific bypass logic" requirement). New `frontend/src/lib/api/scheduling.ts` (`scheduleAppointment`/`rescheduleAppointment`/`cancelAppointment`/`sendReminder`, all take the caller's `authorizedFetch` so the existing bearer-token + single-retry-on-401 flow from 14.1 is reused unchanged, zero duplicated auth logic) + matching types in `lib/api/types.ts`; `parseJsonOrThrow` and a new `AuthorizedFetch` type were exported from `client.ts` for reuse instead of being re-implemented. Shared `frontend/src/components/appointments/appointment-summary.tsx` renders the returned `AppointmentResponse` identically across the schedule/reschedule/cancel result states. `frontend/src/app/portal/page.tsx` now links to all four views instead of showing the old placeholder text. **Flagged, not silently invented (real gaps upstream of this task):** (1) there is no GET endpoint to list a patient's own appointments or list open availability slots anywhere in the backend (`grep`-verified: `scheduling.py` only exposes the four mutating POST routes) -- so every ID field (`patient_id`, `professional_id`, `site_id`, `availability_id`, `appointment_id`) is a plain text input the patient must already know, not a picker/autocomplete; a real self-service UX needs a `GET /appointments` (mine) and a `GET /availability` (open slots) route, both out of this task's literal scope. (2) `TokenResponse` (from 14.1's `/auth/login`) only returns `user_id` and `role`, never `patient_id` -- confirmed via `user_directory.py`'s own comment that `users.patient_id` is a distinct FK column from `users.id` for `role='patient'` rows -- so the portal cannot prefill "your own" patient id from the session; the Patient ID field stays a manual input, same reasoning as 14.1's tenant-id gap. (3) `patient-self-service-portal`'s spec §"Consent Gate Enforced in Portal" (portal MUST block submission and redirect to a consent flow when consent is missing) is NOT implemented here -- verified there is no consent-check endpoint wired into `platform/inbound/api` at all (`ScheduleAppointment`'s own docstring says consent is "checked upstream by the caller", but no caller in the REST router layer does it), and no consent-flow frontend page exists or is scheduled in tasks.md Phase 14 (14.1-14.5 cover auth, these forms, embedded chat, markdown, confirmation UX -- never consent UI); building a fake client-side consent gate against a non-existent backend contract would itself be the kind of divergent/invented behavior the spec's "no channel-specific bypass logic" requirement forbids, so it is left unbuilt and explicitly flagged instead. (4) Cancel and reminder forms submit as a normal single-step web form (no double-confirm dialog) -- this is NOT the chat-style "turn N asks, turn N+1 affirms" pattern, which is 14.5's explicit separate scope; a plain destructive-styled submit button is standard web-form UX, not a divergent business rule. 24 new tests (5 `scheduling.ts` client, 1 `AppointmentSummary`, 4 schedule page, 4 reschedule page, 4 cancel page, 5 reminder page, 1 portal-page nav-links) + all 28 pre-existing 14.1 tests, 52/52 passing; `npm run lint` clean (0 errors, fixed 1 warning: unused `afterEach`/`beforeEach` imports in the new scheduling test file); `npm run build` succeeds (static export emits `out/portal/appointments/{schedule,reschedule,cancel,reminder}.html` alongside the existing routes). No backend changes in this batch. 14.3 (embedded chat), 14.4 (markdown), 14.5 (confirmation UX) remain, separate future batches.
- [x] 14.3 Embedded chat component: `thread_id = crypto.randomUUID()` in-memory only (never `localStorage`/`sessionStorage`/cookies/IndexedDB); SSE via `fetch`+`ReadableStream`.
  **Closed (PR 14 batch C, same work unit as 14.1/14.2, uncommitted -- left in the working tree for review).** New `frontend/src/lib/api/chat.ts`'s `streamChat(authorizedFetch, params, onEvent)`: `fetch` + `ReadableStream` consumer of `POST /chat/stream` (design.md §8.5) -- NOT the native `EventSource`, since that's GET-only and can't carry the message body (design.md's own note). Reuses the caller's `authorizedFetch` (same bearer-token + single-retry-on-401 flow as `scheduling.ts`, no new auth logic). Manually parses the exact `event: {type}\ndata: {json}\n\n` wire format `backend/app/platform/inbound/graph/streaming/sse.py`'s `format_sse_event` writes, buffering partial chunks across `reader.read()` calls so a frame split mid-line by the network still parses correctly (proven by a dedicated test that splits a real frame's bytes across two chunks). Maps all 4 event types verbatim from `chat.py`'s `_stream_turn`/design.md §8.5: `status` (`{phase,label}`), `token` (`{delta}`), `done` (`{audit_ref,calendar_sync_status,finish_reason}`), `error` (design.md §21's envelope: `error_code`/`category`/`user_message`/`retryable`/`correlation_id`). An `error` SSE frame is delivered to the caller via `onEvent`, NOT thrown -- per design.md §21.2 ("los errores a mitad de stream se entregan como evento `error`... el stream se cierra tras el `error`"), the SSE channel itself is the error's delivery mechanism once streaming has started; `streamChat` only throws `ApiError` for a pre-stream-start HTTP failure (e.g. a 401 that survives `authorizedFetch`'s own refresh-retry), matching `chat_stream()`'s own real behavior -- `StreamingResponse` commits HTTP 200 before the generator runs, so every failure AFTER that point is necessarily an `error` SSE frame, never a non-200 stream response. New types added to `frontend/src/lib/api/types.ts`: `ErrorEnvelope`, `ChatStatusEvent`/`ChatTokenEvent`/`ChatDoneEvent`/`ChatErrorEvent`/`ChatStreamEvent` (discriminated union). New `frontend/src/components/chat/chat-widget.tsx`'s `<ChatWidget>`: the actual chat UI, `<RequireAuth>`-wrapped via new `frontend/src/app/portal/chat/page.tsx` (mirrors 14.2's per-view-page pattern). **`thread_id`, in-memory only, exactly as this task and spec `embedded-patient-chat`'s "Thread ID is never persisted client-side" require:** `const [threadId] = useState(() => crypto.randomUUID())` -- design.md §8.6's own literal snippet -- generated once per component mount, held only in React state, sent to the backend as `client_random_uuid` (the real, tenant/user-scoped `thread_id` is assembled server-side from this value plus the caller's authenticated identity, per `chat.py`'s own docstring -- this component never claims to own the "real" `thread_id`, only the client-random half). Verified by a dedicated test that spies on `Storage.prototype.setItem` and asserts it's never called, plus asserts `document.cookie` stays empty, across a full send-message interaction. A page reload unmounts the component (this is a static-export SPA with client-side routing, `frontend/AGENTS.md`) and a remount generates a brand-new UUID -- by construction (React's own lazy-initializer semantics), not an explicit "clear on unload" handler. Intermediate `status` events render as a transient "Consultando disponibilidad"-style line (spec's own "Streaming Responses With Intermediate Status Visibility" requirement); `token` deltas accumulate into a live-updating draft paragraph via a `useRef` mirror (avoids stale-closure reads of the latest accumulated text from inside the `onEvent` callback) and are finalized into the turn list on `done` or `error`. **Rendering is plain text (`whitespace-pre-wrap`), a deliberate, explicitly out-of-scope placeholder** -- tasks.md 14.4 (`react-markdown`+`rehype-sanitize`) is NOT implemented here; the component's own docstring flags this and names 14.4 as the follow-up. **The turn-N/turn-N+1 confirmation-prompt UX (tasks.md 14.5) also is NOT specially handled here, correctly** -- design.md §8.5 itself states a confirmation prompt "viaja... sin ninguna diferencia de protocolo respecto a una respuesta normal", so an ordinary assistant turn already displays it; no extra work was needed or added for that case in this task, and no bulk-affirm/decline UI exists yet (that's 14.5's explicit scope). `frontend/src/app/portal/page.tsx` updated: nav links gained "Chat with Tony" -> `/portal/chat`, replacing the old "embedded chat ships in a later batch" placeholder text. 14 new tests (4 `chat.ts` -- request shape, split-frame status/token/done ordering, error-event-not-thrown, pre-stream 401-throws; 7 `ChatWidget` -- thread-id-never-persisted, same-thread-id-across-turns, empty-message-rejected, status-visibility, token-accumulation-with-status-clear-on-done, error-event-message-display, pre-stream-failure-message; 2 `ChatPage` -- `RequireAuth` redirect, renders-when-authenticated) + 1 updated (`portal/page.test.tsx`'s nav-links assertion) + all 51 pre-existing 14.1/14.2 tests, 65/65 passing. `npm run lint` clean (0 errors, 0 new warnings). `npm run build` succeeds, static export emits `out/portal/chat.html` alongside the existing routes. No backend changes in this batch -- `POST /chat/stream` (task 12.1) and its SSE contract were already real and unchanged. **Flagged, not silently guessed:** the request body field this component sends is `client_random_uuid` (matching `ChatRequest`'s real field name in `chat.py`), even though design.md §8.6's prose calls the frontend-held value `thread_id` -- these are the same concept under two different names at two different layers (client-random half vs. server-assembled real key); this closure note and the component's own docstring make that mapping explicit rather than leaving it implicit. Same honest scope note `chat.py`'s own docstring already carries applies here too: because no LLM adapter in this codebase streams incrementally yet, `token` events arrive as the graph's final text re-segmented into sentence-boundary units rather than concurrent-with-generation deltas -- this component's incremental-append rendering already handles that transparently (it has no way to distinguish "true" from "re-segmented" streaming, by design) and needs zero changes the day a real `.astream()` adapter lands.
- [x] 14.4 `react-markdown` + `rehype-sanitize` rendering of Tony responses.
  **Closed (PR 14 batch D, same work unit as 14.1/14.2/14.3, uncommitted -- left in the working tree for review, same convention as A/B/C).** `frontend/src/components/chat/chat-widget.tsx`'s `ChatWidget` now renders every **assistant** turn (finalized messages and the in-progress streaming draft alike) through a new shared `MarkdownMessage` sub-component: `<ReactMarkdown rehypePlugins={[rehypeSanitize]}>{text}</ReactMarkdown>`, replacing the `whitespace-pre-wrap` plain-text paragraphs 14.3 deliberately left as a flagged placeholder. Matches design.md §8.8 verbatim: `react-markdown` (no `dangerouslySetInnerHTML`) + `rehype-sanitize` because the content is LLM-generated and therefore an XSS vector. **`react-markdown`/`rehype-sanitize` were already present in `frontend/package.json` and `node_modules`/`package-lock.json`** (added in an earlier commit `7d479bb feat(frontend): add markdown rendering deps and switch to static export`, before any frontend UI existed) -- verified via `Read`, no `npm install` needed this batch. **User turns are deliberately left as plain `whitespace-pre-wrap` text, not Markdown** -- design.md §8.8 and spec `embedded-patient-chat`'s "Markdown-Formatted Responses" requirement both scope Markdown rendering to Tony's (the LLM's) responses only; the patient's own typed input has no reason to be interpreted as Markdown and doing so would be inventing unrequested behavior. **Sanitization schema decision, explicit not silently assumed:** `rehype-sanitize` is used with its **default schema** (`hast-util-sanitize`'s `defaultSchema`, no custom `schema` prop) -- design.md §8.8 says only "se sanitiza con `rehype-sanitize` (allowlist)" without naming custom allowed tags/attributes, and Tony's system prompt (task 12.5) only asks for "encabezados, listas, negrita, bloques de codigo," all of which the default schema already allows; inventing a narrower custom schema would be scope creep with no spec basis, and CommonMark's own block/inline set (headings, lists, bold, code, links, paragraphs) is already covered. **Two RED-driven tests added, both genuinely failed before this task's implementation and pass after (verified, not assumed):** (1) a positive test proving `**bold**`/`[text](url)`/`- list` Markdown source renders as real `<strong>`/`<a href=...>`/`<li>` DOM elements (not the raw markdown syntax as literal text) -- this is the direct RED driver, since the pre-14.4 `whitespace-pre-wrap` renderer never produced these element types. (2) a combined malicious-markup test asserting a `javascript:` URI in a Markdown link (`[click here](javascript:window.__pwned=true)`) has its `href` attribute **stripped entirely** (`getAttribute("href")` is `null`, not merely non-`javascript:`) by `rehype-sanitize`'s default `protocols.href` allowlist (`http`/`https`/`irc`/`ircs`/`mailto`/`xmpp` -- verified directly against `hast-util-sanitize`'s `defaultSchema` via a throwaway Node script rather than assumed) -- this is the test's real RED-driving assertion, since `react-markdown` alone (no sanitize) would leave the dangerous `href` intact; a `window.__pwned` global-pollution check confirms no script ever actually ran. **Flagged, deliberately NOT a separate RED-driving test case:** a raw `<script>` tag embedded directly in Markdown source (e.g. `Hola <script>...</script> hay turnos.`) is folded into the SAME test as an additional defense-in-depth assertion (`document.querySelector("script")` stays `null`) rather than its own standalone test, because it passes trivially both before AND after this task -- `react-markdown` never parses raw HTML embedded in Markdown source into real elements unless the `rehype-raw` plugin is added (NOT used here, and not requested by design.md), so the raw tag is dropped outright by `react-markdown` itself, independent of `rehype-sanitize`; keeping it as a lone "RED" test would have been a banned trivial/vacuous assertion per strict-TDD rules (confirmed empirically: it already passed against the pre-14.4 code, for an unrelated reason -- React's own JSX text-escaping). This is intentionally the more conservative, safer configuration (no raw-HTML rendering capability at all, `rehype-sanitize` as a second independent layer on top) rather than the riskier `rehype-raw` + `rehype-sanitize` combination, which design.md does not ask for. 2 new tests in `frontend/src/components/chat/__tests__/chat-widget.test.tsx` + all 7 pre-existing `ChatWidget` tests (unmodified) + all 65 other pre-existing 14.1/14.2/14.3 tests, 67/67 passing (safety-net baseline before this task: 7/7 `ChatWidget` tests green, confirmed first). `npm run lint` clean (0 errors, 0 warnings). `npm run build` succeeds, static export unchanged in shape (`out/portal/chat.html` still emitted, same route). No backend changes in this batch. 14.5 (confirmation-prompt UX) remains, a separate future batch, not started here.
- [x] 14.5 Confirmation-prompt UX (turn N asks, turn N+1 affirms/declines) as a normal chat turn.
  **Closed (PR 14 batch E, same work unit as 14.1/14.2/14.3/14.4, uncommitted -- left in the working tree for review, same convention as A/B/C/D). Phase 14 is now COMPLETE (14.1-14.5).** **Zero implementation files changed -- confirmed, not assumed, by re-reading every relevant source before concluding.** Re-verified against three independent sources: (1) `design.md` §8.5 states the confirmation prompt, once `confirmation_gate` sets `confirmation=needed`, "viaja... `status → token* → done` — sin ninguna diferencia de protocolo respecto a una respuesta normal" (travels with no protocol difference from a normal response) -- the mutation itself doesn't happen in turn N, and turn N+1 is a brand-new `graph.astream()` invoke over the SAME `thread_id` that resolves the pending `proposed_action` from the checkpoint server-side (`route_from_start` -> `confirmation_gate`), entirely opaque to the client. (2) spec `embedded-patient-chat`'s "Confirmation Required Before Any Mutating Action" requirement and its 4 scenarios (single-booking-still-requires-confirmation, patient-declines, read-only-skips-confirmation, confirmation-composes-with-streaming) describe only conversational content and turn sequencing -- none names a distinct confirm/decline UI widget, a special SSE event type, or an `awaiting_confirmation`-style response field. (3) `backend/app/platform/inbound/api/routers/chat.py`'s `_stream_turn`/`ChatDoneEvent` shape (`audit_ref`/`calendar_sync_status`/`finish_reason` only) confirms there is no such field to surface even if one wanted to build a visual cue -- `grep`-verified directly against the router source, not inferred from docs alone. `frontend/src/components/chat/chat-widget.tsx` (built in 14.3, Markdown-rendered since 14.4) already sends every user message -- affirmation, decline, or anything else -- through the exact same `handleSend` -> `streamChat(authorizedFetch, { message, clientRandomUuid: threadId }, handleStreamEvent)` path with the same in-memory `threadId` held across the component's lifetime, and already renders every assistant reply (a confirmation question, its own turn's answer) as an ordinary list item via the shared `MarkdownMessage` component; this was already noted as correctly-scoped in 14.3's own closure note before 14.5 existed as a batch. **Two new approval tests added instead** (`strict-tdd.md`'s "Approval Testing" pattern, explicitly sanctioned for this batch since no production code needed writing): `frontend/src/components/chat/__tests__/chat-widget.test.tsx` gained "renders a confirmation prompt as an ordinary assistant turn, then the user's plain-text affirmation on the next turn through the exact same send path" (asserts: turn N's confirmation text renders as a normal assistant turn; no confirm/decline button exists anywhere in the DOM; the send button stays enabled; turn N+1's plain "Sí, confirmo" is sent via `streamChat` with the IDENTICAL call shape and the SAME `clientRandomUuid` as any other turn; turn N+1's own reply renders as a normal assistant turn; turn N's original prompt is still visible afterward, untouched) and "also renders a decline exchange as ordinary chat turns, reusing the identical send path with no appointment-specific branching" (same shape, "No, dejalo" instead of an affirmation, proving the mechanism is generic to any turn N+1 text rather than hardcoded to an affirmative case -- this is the task's triangulation pass, covering spec's "Patient declines the proposed action" scenario). Both tests PASS immediately against the pre-existing, unmodified `chat-widget.tsx` (proven by running the test file both before and after adding them, 9/9 then 11/11, with zero production-code diff in between) -- this is the expected, correct outcome of an approval test proving already-satisfied behavior, not a Strict-TDD violation: `strict-tdd.md`'s Three Laws bar writing PRODUCTION code before a failing test exists, not writing VALIDATION tests for behavior that already exists and needs no new code. Safety-net baseline confirmed first (9/9 pre-existing `ChatWidget` tests green before this batch). Full suite: 69/69 passing (67 pre-existing 14.1-14.4 tests + 2 new). `npm run lint` clean (0 errors, 0 warnings). `npm run build` succeeds, static export unchanged in route shape (`out/portal/chat.html` still the only chat route, same as 14.3/14.4). No backend changes in this batch. **Flagged, not silently assumed:** this closure does not claim the backend's `confirmation_gate`/`hitl_approval` composition (design.md §8.9/§10.4) is itself fully implemented or tested end-to-end -- that is backend graph scope from earlier phases (Phase 8-12), out of tasks.md 14.5's literal frontend scope, which is specifically about the CLIENT not needing a special UI mode. Phase 14 (Frontend — Patient Portal & Embedded Chat) is now fully closed: 14.1 auth, 14.2 self-service forms, 14.3 embedded chat + SSE, 14.4 Markdown rendering, 14.5 confirmation-prompt UX (this task) all done. Phase 15 (staff copilot dashboard) is untouched, a separate future work unit.

- [x] 14.6 [gap closure] Consent gate enforcement for `patient-self-service-portal` (closes sdd-verify CRITICAL, verify-report id #414).
  **Closed (cross-cutting bugfix, NOT a pre-planned task -- retroactively closing a PR10-era gap.** Today's `sdd-verify` pass on Phase 14 (verify-report #414) found spec `patient-self-service-portal`'s MUST-level "Consent Gate Enforced in Portal" requirement (scenario "Pending consent blocks form submission") was unimplemented full-stack: `backend/app/platform/inbound/api/routers/scheduling.py` (task 10.1, PR 10, already merged to `main` long before Phase 14) never called `CheckConsent` at all, even though `ScheduleAppointment`'s own docstring always claimed consent was "checked upstream by the caller." The chat channel's equivalent (`platform/inbound/graph/nodes/consent_gate.py`, task 11.2, PR 11) DID get built correctly -- this router's own gap simply went uncaught by every earlier verify pass (PR10's own verify never checked for it) until today. This closure fixes the dangling contract; it does not touch task 11.2's own already-correct chat-channel implementation. **Backend** (`backend/app/platform/inbound/api/routers/scheduling.py`, `backend/app/composition_root.py`, `backend/app/modules/governance/consent/domain/errors.py` (new), `backend/app/platform/inbound/api/errors.py`): wired the SAME `CheckConsent` use case (`governance/consent`, design.md §11) into all 4 mutating routes (schedule/reschedule/cancel/reminder -- `consent_gate.py`'s own docstring lists exactly these 4 as patient-data-touching), following that node's already-established deny-by-default pattern (only `ConsentCheckResult.CURRENT` passes). New `build_check_consent`/`build_scheduling_repository` factories in `composition_root.py`, mirroring the existing `build_*` convention. New `ConsentNotCurrentError` (subclasses `DomainError` directly, same reasoning as `RateLimitExceededError` -- consent is its own governance plane, not RBAC's). **Order per route, deliberately NOT the chat graph's edge order:** `_require_authorized` (RBAC, duplicating what the use case's own `AuthorizeAction.execute()` will do again internally) runs FIRST, then `patient_id` resolution (request field for `schedule`; an RLS-scoped `get_appointment` read for `reschedule`/`cancel`/`reminder`, which never carry `patient_id` as a request field -- resolving it from a client-supplied field would let a tampered field bypass the check for the WRONG patient), then the consent gate, then the actual use case. This ordering keeps RBAC-denial-takes-priority (the router's own pre-existing, already-verified test expected this) and never introduces a 404-vs-403 appointment-existence oracle for an unauthorized actor (mirrors each use case's own internal `authorize() -> get_appointment()` order exactly). **Error shape chosen:** a NEW 7th error-taxonomy category, `consent-required`/`error_code: "consent_required"`, HTTP 403, `retryable: false` -- deliberately not folded into `auth` (RBAC's category) since consent and RBAC are established as independent governance planes throughout this codebase. Spec `platform-hardening`'s error-taxonomy requirement only mandates its 5 named categories "at minimum," so this is a documented, deliberate extension of design.md §21.1's table (flagged there and in `errors.py`'s own docstring as needing a future design.md revision to fold the row in formally, not silently left out of sync). **Language flag, not silently decided:** this new category's `user_message` ("You must accept the informed consent before continuing.") is English, unlike every other entry in `_MAPPINGS` (Spanish, presumably per `openspec/config.yaml`'s carve-out for end-user text the business specifically requested in Spanish) -- chosen to match the frontend's own all-English UI copy per this task's explicit instruction; flagged in `errors.py`'s own docstring as a genuine pre-existing language inconsistency across the table worth a future product/i18n decision, not resolved wholesale here. **Frontend** (`frontend/src/app/portal/appointments/{schedule,reschedule,cancel,reminder}/__tests__/page.test.tsx`): investigated first whether `ErrorEnvelope`/`ApiError` (`lib/api/types.ts`/`client.ts`) needed any change to carry the new `error_code`/`category` -- they did not; both were already generic enough (`ErrorEnvelope`'s fields are untyped `string`, `ApiError` already surfaces `body.user_message` verbatim via `.message`), confirmed by reading both files, no type changes made. **Zero frontend production code changed** -- all 4 appointment forms (built in 14.2) already display any `ApiError.message` verbatim in a destructive `Alert` on submission failure; the new backend error flows through that exact same, already-existing generic passthrough with no special-casing needed. Added one new regression test per form (4 total) pinning this specific scenario (403/`consent_required` -> curated message shown, submission blocked) to the spec's own scenario name, rather than relying only on the pre-existing generic "shows the backend's error message on failure" test to imply coverage. **Did NOT build a consent-granting UI** -- investigated `governance/consent`'s ports/registry for any existing way to actually accept/record consent from a channel; none exists (`ConsentRegistryPort` only exposes reads: `get_current_policy`/`get_latest_consent`/`get_consent_check_data`, no write method). Building a frontend page against a non-existent backend endpoint would itself be the "channel-specific bypass logic" / invented-behavior problem this change's own conventions forbid, so the correct scope for THIS fix is a clear blocking message only; the spec scenario's "redirect to the consent flow" clause is therefore satisfied only in the "block submission" half -- **flagging a genuinely NEW, separate gap: no consent-granting/consent-flow endpoint or UI exists anywhere in this codebase yet** (out of this task's scope to invent one; a future task should design that flow, likely a dedicated `AcceptConsent` use case + `POST /consent/accept` route + a real consent-flow frontend page, before this MUST-level requirement's "redirect to the consent flow" half can be honestly closed). **Tests, STRICT TDD, genuine RED confirmed by reverting the implementation and re-running (not assumed):** backend -- `backend/tests/platform/inbound/api/routers/test_scheduling_router.py` gained 6 new tests (schedule/reschedule/cancel/reminder each denied 403 `consent_required` when the patient has no current consent; one positive-path test proving a patient WITH current consent passes reschedule-then-cancel; the pre-existing `succeeds_end_to_end` test updated to seed consent, matching the spec scenario's own GIVEN clause) plus 2 new conftest helpers (`seed_current_consent`, `seed_scheduled_appointment`); confirmed RED first by `git stash`-reverting `scheduling.py`/`composition_root.py`/`errors.py` and re-running -- exactly the 4 new denial tests failed (200 instead of 403), the other 4 (RBAC-denied, not-found, and the two now-consent-seeded tests) stayed green since the old router never checked consent at all; restored and re-ran GREEN, 8/8. Full backend suite: 815 passed, 10 skipped (no regressions), `uv run lint-imports`: 3/3 contracts kept. Frontend: 73/73 passing (69 pre-existing + 4 new), `npm run lint` clean (0 errors). **Local environment discovery, not a code issue:** this session's Postgres connection on host port 5432 was being intercepted by a native Windows PostgreSQL 18 service also bound to that port (Docker's own container was healthy and correctly configured, confirmed via `docker exec`/container-internal TCP tests) -- worked around for this session only via the `docker-compose.override.yml`'s alternate `5433` host mapping (`DATABASE_URL`/`RUNTIME_DATABASE_URL` env overrides at test-invocation time only, no `.env.local`/config.py change), not a project code change.

## Phase 15: Frontend — Staff Copilot Dashboard

- [x] 15.1 Staff login (reception/professional/admin), `allowed_actions`-driven UI.
  **Closed (PR 15, uncommitted -- left in the working tree for review, same convention as PR 14's batches).** `frontend/src/app/staff/login/page.tsx` -- same `/auth/login` endpoint the patient `/login` page uses (`auth.py` has no staff-specific login route; `TokenResponse.role` is the only signal that distinguishes a staff account). New `frontend/src/lib/auth/roles.ts` (`STAFF_ROLES`/`isStaffRole`, mirroring `chat.py`'s own `_STAFF_ROLES` frozenset -- a deliberately duplicated constant across the frontend/backend boundary, not a cross-import, same posture `lib/api/types.ts` already takes toward backend Pydantic shapes). A patient credential set that successfully authenticates is explicitly REJECTED by this page (logged back out immediately, "This portal is for staff accounts only" shown) rather than silently landing in the staff dashboard. **Reused, not duplicated:** extracted the patient `/login` page's card/form markup into a new shared `frontend/src/components/auth/credentials-login-card.tsx` (`<CredentialsLoginCard>`), now the presentational shell for BOTH `/login` and `/staff/login` (`frontend/AGENTS.md`: "extract repeated UI into shared components... avoid duplicated implementations") -- `/login/page.tsx` refactored to consume it, safety-net-verified unchanged (4/4 pre-existing tests green before AND after). New `frontend/src/lib/auth/require-staff-auth.tsx` (`<RequireStaffAuth>`) layers a role check on top of the EXISTING `<RequireAuth>` (extended with an optional `redirectTo` prop, default `"/login"` unchanged for every pre-existing caller, so `<RequireStaffAuth>` can pass `"/staff/login"`) -- an authenticated PATIENT session hitting a `/staff/**` route is logged out and redirected, never rendered staff content. `frontend/src/app/staff/page.tsx` is the staff-side equivalent of `/portal/page.tsx`, greeting by role and linking to `/staff/chat` (15.3) and `/staff/registry` (15.2, see that task's own gap note). **`login()`'s contract changed** (`lib/auth/auth-context.tsx`): now resolves with the freshly authenticated `AuthUser` instead of `Promise<void>`, so `/staff/login` can branch on the just-resolved role directly in its own event handler -- avoids a stale-closure/`useEffect`-timing hazard (reading this component's own `user` from context immediately after `await login(...)` would still reflect the PRE-login render) that an earlier draft of this task hit as a real `eslint-plugin-react-hooks` `set-state-in-effect` error, fixed by this contract change rather than suppressed. **`allowed_actions`-driven UI, flagged gap, not silently narrowed:** the dashboard's nav links are gated only by "is this user a staff member at all" (`role ∈ {reception, professional, admin}`), the coarsest signal `TokenResponse.role` already provides -- there is NO backend endpoint exposing `ListAllowedActions`/`allowed_actions` to the frontend (`AuthorizationPort`/`ListAllowedActions` today only feed `resolve_toolset` inside the LangGraph copilot, design.md §5.4; `grep`-verified against every router in `platform/inbound/api/routers/`, none exposes it). Building fine-grained per-action nav gating against a non-existent contract would itself be invented behavior; a future `GET /me/permissions`-shaped endpoint wrapping `ListAllowedActions` is the real fix, not built here. 28 new tests (4 `roles.ts`, 5 `RequireAuth`'s new `redirectTo` prop + its 4 pre-existing cases, 3 `RequireStaffAuth`, 5 `CredentialsLoginCard`, 4 `/staff/login`, 3 `/staff` dashboard, 1 `auth-context`'s new login-resolves-user contract) + all pre-existing tests green throughout (safety nets confirmed BEFORE each modification: `RequireAuth` 4/4, `/login` page 4/4, `auth-context` 5/5). `npm run lint` clean (0 errors -- one real `react-hooks/set-state-in-effect` error surfaced and fixed via the `login()` contract change above, not suppressed). `npm run build` succeeds, static export emits `out/staff/{login,index}.html` alongside the existing routes.
- [ ] 15.2 Staff registry + shift management views. **BLOCKED -- flagged gap, not silently invented; see closure note below.**
  **Investigated, not built (PR 15, same session as 15.1/15.3).** Per this task's own explicit instruction ("no new backend endpoints... if an endpoint you need doesn't exist, flag it explicitly, don't invent one"): confirmed via `app/main.py`'s `create_app()` (`grep`-verified, only 4 `include_router` calls) that `backend/app/platform/inbound/api/routers/` has EXACTLY `auth`, `appointments` (scheduling), `calendar/oauth`, and `chat` -- **there is no REST route for staff or shift data at all.** Not create, not deactivate, not edit, and not even a read/list endpoint: `StaffRepositoryPort`/`ShiftRepositoryPort` (`backend/app/modules/staff/application/ports/driven/`) don't expose a `list_*` method either, so a GET endpoint isn't even a thin wrapper away -- the domain layer itself has no "list staff at this site" or "list a professional's shifts" capability yet. The 4 use cases DO exist and ARE wired (`RegisterStaff`/`DeactivateStaff`/`CreateShift`/`EditShift`, dispatched via `platform/inbound/graph/nodes/persist_and_audit.py`'s `staff:register`/`staff:deactivate`/`shift:create`/`shift:edit` `ActionKey`s) but ONLY reachable through the LangGraph copilot (15.3), never a direct HTTP call. This is categorically different from tasks.md 14.2's flagged gap (a missing GET/list convenience alongside four REAL, working POST routes) -- here there is ZERO REST surface for this capability, on either the read or the write side. Building a form that submits to a non-existent endpoint would be dead UI with no real backend behind it; per this change's own "no channel-specific bypass logic" / "don't invent behavior" convention (applied identically in 14.6's own consent-flow gap note), a fake CRUD view was deliberately NOT built. **What WAS built instead:** `frontend/src/app/staff/registry/page.tsx`, a real, tested, `<RequireStaffAuth>`-wrapped page that honestly states the gap (`role="alert"`, "Not available yet... only the internal copilot chat can reach these actions today") and links to `/staff/chat` as the one currently functional path to these 4 use cases for an authorized staff user (same `AuthorizeAction`/RLS enforcement a future direct route would also need) -- explicitly NOT counted as closing this task, since no actual registry/shift viewing or management occurs. **Recommended follow-up** (backend scope, not built here): `backend/app/platform/inbound/api/routers/staff.py` mirroring `scheduling.py`'s shape (`POST /staff/register`, `POST /staff/{id}/deactivate`, `POST /staff/{id}/shifts`, `PATCH /staff/shifts/{id}`) wired to the composition root's already-existing `build_register_staff`/`build_deactivate_staff`/`build_create_shift`/`build_edit_shift` factories, PLUS new `list_by_site`-shaped repository methods and a `GET /staff`/`GET /staff/{id}/shifts` pair -- none of which exist today. 3 new tests (`frontend/src/app/staff/registry/__tests__/page.test.tsx`: unauthenticated redirect, honest gap notice with no fake form rendered, link to `/staff/chat`) prove the gap page's own real, scoped-down behavior -- these do NOT constitute closing 15.2's literal scope.
- [x] 15.3 Internal copilot chat reusing the patient chat's `thread_id`/SSE/confirmation pattern, keyed by `staff_user_id`.
  **Closed (PR 15, same session as 15.1).** `frontend/src/app/staff/chat/page.tsx` reuses `<ChatWidget>` (`components/chat/chat-widget.tsx`, built in PR 14's 14.3/14.4) **VERBATIM -- zero changes to that component.** Confirmed by reading `chat.py` closely first (per this task's own instruction): `ChatWidget` was already fully channel-agnostic from 14.3 onward -- it only calls `useAuth().authorizedFetch` + `streamChat(...)` against `POST /chat/stream`, the SAME endpoint for both `patient_chat` and `staff_copilot`. The channel distinction (design.md §8.6: "mismo patron... la unica diferencia es que el `user_id` en la key es el del staff") is resolved entirely server-side from the caller's authenticated `role` (`chat.py`'s `_channel_for`/`_STAFF_ROLES`) -- there is no client-sent `channel` field for this page to declare, and the `thread_id` mechanism (in-memory `crypto.randomUUID()` per mount, sent as `client_random_uuid`, server-assembles `"{tenant_id}:{staff_user_id}:{client_random_uuid}"`) is identical to the patient flow by design.md's own "mismo mecanismo" paragraph -- confirmed nothing needed to change there either. The only new code is the route guard: `<RequireStaffAuth>` instead of `<RequireAuth>`, so an authenticated PATIENT session is redirected to `/staff/login` rather than rendering the copilot (a patient reaching `/staff/chat` would otherwise still work end-to-end against the backend, since `chat.py` itself has no route-level RBAC check -- `RequireStaffAuth` is a frontend-only UX guard, not a substitute for the backend's own per-action `rbac_gate` enforcement inside the graph, consistent with spec `internal-staff-copilot`'s "Domain-Layer Enforcement, Not UI-Only" requirement). 3 new tests (`frontend/src/app/staff/chat/__tests__/page.test.tsx`: unauthenticated redirect, patient-session redirect, authenticated staff renders the same `ChatWidget` -- message input + send button + "Chat with Tony" title). No backend changes in this batch -- `POST /chat/stream`'s staff-vs-patient channel routing (task 12.1/`chat.py`) was already real and unchanged, confirmed via direct source read, not assumed. **Full-suite status for the batch closing 15.1/15.2(gap)/15.3 together:** frontend 100/100 passing (73 pre-existing + 27 new across `roles.ts`/`require-auth`/`require-staff-auth`/`credentials-login-card`/`auth-context`/`/login`/`/staff/login`/`/staff`/`/staff/chat`/`/staff/registry`), `npm run lint` clean (0 errors), `npm run build` succeeds (static export emits `out/staff/{index,login,chat,registry}.html` alongside every pre-existing route). Backend: zero backend files touched this batch (`git status` confirmed) -- the full `uv run pytest` suite was not re-run because the local `docker-compose` LocalStack container failed to start this session (exit code 55, a pre-existing local-environment issue unrelated to this frontend-only batch, distinct from PR 14's own previously-documented port-5432-conflict gotcha) and there is zero backend code in this diff for it to have regressed.

## Phase 16: AWS Deployment + Local Dev Parity

- [x] 16.1 Pick an IaC tool (Terraform/CDK/CloudFormation — **not fixed by design.md**, decide before this task) and author VPC/subnets/SG/ALB/WAF/ECS Fargate/RDS Single-AZ/NAT/Secrets Manager/IAM per §20. Include a bootstrap step that runs `CREATE EXTENSION IF NOT EXISTS pgcrypto` and `CREATE EXTENSION IF NOT EXISTS btree_gist` as the RDS master user before the first `alembic upgrade head` — both are on RDS Postgres's trusted/allow-listed extension set (no special AWS enablement needed), but nothing outside `infra/postgres/init/` (docker-compose-only) creates them today (PR 2 review finding).
  **Closed (PR 16, branch `pr-16-aws-deployment-parity`, uncommitted — left in the working tree for review). Tool: Terraform** (decided this task, per this task's own explicit prompt — not CDK/CloudFormation; no re-litigation needed downstream). New `infra/terraform/` tree, structured as `modules/{network,secrets,rds,alb-waf,ecs,scheduler-alarms,frontend-cdn}/` + `envs/prod/` (root composition) — `infra/localstack/`/`infra/postgres/init/` (local dev, pre-existing) untouched, matching design.md §22's "local dev never uses Terraform/real AWS" split. `modules/network`: VPC (2 AZ), public subnets (ALB+NAT only)/private subnets (ECS+RDS), single NAT Gateway (design.md §20.2's explicit cost tradeoff), 3 least-privilege SGs (ALB `0.0.0.0/0:443`, ECS only-from-ALB, RDS only-from-ECS:5432 — no `0.0.0.0/0` anywhere else). `modules/alb-waf`: ALB + HTTP→HTTPS redirect listener + HTTPS listener (ACM cert, out-of-band) + target group (Fargate `ip` target type) + `aws_wafv2_web_acl` rate-based rule (design.md §19 layer 1). `modules/rds`: RDS Postgres Single-AZ, `storage_encrypted=true`, `multi_az=false` (design.md ADR: Single-AZ risks availability not durability, PITR via `backup_retention_period` covers durability), `deletion_protection=true`, `db.t3.medium` default (design.md §20.5's own connection-budget math). `modules/secrets` + `modules/ecs`'s IAM: execution role (ECR pull + CloudWatch Logs + `secretsmanager:GetSecretValue` on every secret injected via a task definition's `secrets` block) separate from the task role (only what the APP ITSELF calls via boto3 at runtime — KEK read for `AesGcmVault`, `cloudwatch:PutMetricData` for the two scheduled jobs). **Bootstrap step, this task's own explicit requirement:** a dedicated one-off `aws_ecs_task_definition.bootstrap_db` (NOT a Terraform `null_resource`/`local-exec` — Terraform itself typically runs OUTSIDE the VPC and cannot reach RDS directly, `Database not publicly reachable` per design.md §20, so the bootstrap must be an in-VPC ECS task, invoked via `aws ecs run-task` per `infra/terraform/README.md`'s runbook) using the plain `postgres:16` image (has `psql`, matches `docker-compose.yml`'s own local Postgres image — no new backend code needed) running `CREATE EXTENSION IF NOT EXISTS pgcrypto`/`btree_gist` AND creating the `app_runtime` role (mirrors `infra/postgres/init/02_app_runtime_role.sql`, whose own comment explicitly deferred that step to "Phase 16... the role creation belongs next to it" — honored here, not silently dropped) BEFORE the first `alembic upgrade head` (a separate `aws_ecs_task_definition.migrate`, same ordering dependency, documented not automated — see README's apply-order runbook). **Credentials decision, flagged not silently assumed:** used Terraform-generated `random_password` for the RDS master + `app_runtime` passwords (composed into `DATABASE_URL`/`RUNTIME_DATABASE_URL` Secrets Manager secrets) instead of AWS's more modern `manage_master_user_password=true`, because `backend/app/config.py`'s `Settings.database_url`/`runtime_database_url` (and `app/db.py`'s unconditional-at-import-time engine construction) expect ONE composed DSN string via a plain env var — decomposing that to fetch-and-assemble from Secrets Manager JSON at runtime is backend-code scope explicitly out of this IaC-only task. Known, documented tradeoff: this puts plaintext DB credentials in Terraform state, which is why `envs/prod/backend.tf`'s remote-state config (S3+DynamoDB, commented out pending an operator-provisioned bucket) is flagged as MANDATORY before any real `apply`. **Anthropic API key**, not named anywhere in design.md §20's Secrets Manager table, added to `modules/secrets` as a flagged real omission (the LLM provider has no secret without it). ALB health check path (`/openapi.json`) is an interim target — no dedicated `GET /health` endpoint exists in `backend/app` yet, flagged in `modules/alb-waf`'s own variable description rather than silently inventing a new backend route in an infra-only task.
- [x] 16.2 EventBridge Scheduler + ECS scheduled tasks: hash-chain verify job (tamper alarm + heartbeat), calendar retry job. Ref §4.3, §7.5.
  **Closed, infra shape built; REAL BACKEND GAP FLAGGED, not papered over.** `modules/ecs` defines two scheduled-task task definitions (`verify_audit_chain`, `retry_calendar_sync`, same backend image as the API service per design.md §1/§20.1 "misma imagen") + `modules/scheduler-alarms` wires `aws_scheduler_schedule` (EventBridge Scheduler, hourly verify / 15-min retry defaults, design.md §4.3/§7.5's own cadence language) targeting `ecs:RunTask` in the private subnets, via a dedicated scheduler IAM role (`ecs:RunTask` + `iam:PassRole` only, least-privilege). **Investigated before wiring the container `command`, per this codebase's own established "flag, don't invent" convention (14.2/14.6/15.2's own closure notes):** grepped the entire `backend/app/` tree for any hash-chain-verify entrypoint — none exists, not even a use case (unlike the calendar retry job, this one has ZERO application-layer implementation anywhere, not just a missing CLI wrapper). The calendar retry job DOES have a real, unit-tested use case (`RetryPendingCalendarSyncs`, tasks.md 9.5, `backend/app/modules/calendar/application/use_cases/retry_pending_calendar_syncs.py`) but no composition-root builder, no CLI entrypoint, and no per-tenant iteration loop (`execute(tenant_id)` takes ONE tenant at a time — an unattended scheduled job needs to enumerate all tenants itself, which nothing does today). Both task definitions' `command` are Terraform variables (`verify_audit_chain_command`/`retry_calendar_sync_command`, default `["python", "-m", "app.jobs.verify_audit_chain"]`/`["python", "-m", "app.jobs.retry_calendar_sync"]`) — placeholders shaped like the entrypoint this infra expects, explicitly documented in `modules/ecs/variables.tf`'s own comment as NOT existing yet, not fabricated backend code. **Recommended follow-up (backend scope, not built here):** an `app/jobs/` package wrapping (a) a new hash-chain verify use case reading `audit_logs` per-tenant by `seq`, recomputing `row_hash`, publishing `AuditChainTamper`/`AuditChainVerifyHeartbeat` custom CloudWatch metrics, and (b) a thin composition-root-wired wrapper around the existing `RetryPendingCalendarSyncs` that loops every active tenant. **Per-tenant alarm dimensioning, flagged simplification:** design.md §4.3 says these metrics carry a `tenant_id` dimension; static Terraform cannot enumerate an unbounded, runtime-created tenant list to generate one alarm per tenant, so `modules/scheduler-alarms` defines one AGGREGATE alarm per metric instead (documented in that module's own top-of-file comment) — catches "some tenant's chain is tampered"/"the job stopped running for everyone," not "which tenant" without inspecting the metric's own datapoints after the alarm fires.
- [x] 16.3 CloudWatch alarms + SNS topic (`AuditChainTamper`, `AuditChainVerifyHeartbeat` with `treatMissingData=breaching`). Ref §4.3.
  **Closed (PR 16, same session as 16.1/16.2).** `modules/scheduler-alarms`: `aws_sns_topic` (`kureha-{env}-audit-alerts`, mirrors `infra/localstack/init/03_sns.sh`'s `kureha-audit-alerts-dev` naming) with an optional email subscription (`var.alarm_email`, left unset by default rather than hardcoding a real on-call address into version control) + two `aws_cloudwatch_metric_alarm` resources exactly matching design.md §4.3/ADR-19's two distinct alarms: `AuditChainTamper` (`GreaterThanOrEqualToThreshold` 1, `treat_missing_data="notBreaching"` — no datapoint just means no tamper reported that period) and `AuditChainVerifyHeartbeat` (`LessThanThreshold` 1, `treat_missing_data="breaching"` — the dead-man's-switch requirement itself: "el verificador se murio" must alert too, not fail silently; alarm period defaults to 2x the verify job's own schedule interval per design.md's "~2x el intervalo" language, both wired as Terraform variables so they stay in sync if the schedule cadence changes). **Metric namespace** (`Kureha/Audit`) is NOT named anywhere in design.md §4.3 — chosen here and documented in `modules/scheduler-alarms/variables.tf`'s own comment as a contract the future verify-audit-chain job's own metric-publish call (16.2's flagged gap) MUST match exactly, or these alarms will never see real data. Both alarms will fire/stay in ALARM state immediately upon a real `apply` until 16.2's backend job actually exists and starts publishing — an honest, expected consequence of building the alerting infra ahead of the application code it observes, not a bug in the Terraform.
- [x] 16.4 S3+CloudFront frontend tier static-export deploy pipeline. Ref §20.1.
  **Closed (PR 16, same session).** `modules/frontend-cdn`: private S3 bucket (versioned, `aws_s3_bucket_public_access_block` fully blocking public ACLs) + CloudFront distribution reading it via Origin Access Control (SigV4, `aws_s3_bucket_policy` scoped to `AWS:SourceArn = this distribution only`) — matches design.md §20.1's "CloudFront sirve solo assets estaticos... no se enruta trafico de API por CloudFront" exactly: no API/SSE traffic behavior configured, this tier is 100% independent of `modules/alb-waf`. **Genuine routing gap found and fixed, not assumed to just work:** actually ran `npm run build` (frontend/, this session) and inspected the real static-export output — Next.js `output: "export"` emits per-route `<route>.html` files (e.g. `out/staff/chat.html`, `out/portal.html`), NOT `<route>/index.html`; a bare CloudFront/S3 request for `/staff/chat` (no extension) would 404 without a rewrite. Added an `aws_cloudfront_function` (viewer-request, `cloudfront-js-2.0`) appending `.html` (or `index.html` for a trailing-slash path) to extension-less URIs before the S3 origin sees the request — the standard documented pattern for a Next.js static export behind CloudFront, validated directly against this session's real `out/` listing (`index.html`, `404.html`, `login.html`, `portal.html`, `portal/chat.html`, `staff.html`, `staff/chat.html`, `staff/login.html`, `staff/registry.html` — all matched by the rewrite logic). `custom_error_response` maps both a bucket-level 403 (OAC-restricted bucket returns 403, not 404, for a missing key) and a real 404 to the export's own `404.html`. Deploy pipeline itself (`npm run build && aws s3 sync out/ s3://<bucket>/ --delete` + CloudFront invalidation) is documented as a CI/CD step in `infra/terraform/README.md`'s runbook, not a Terraform resource — Terraform provisions the tier, it does not own application deploys.
- [x] 16.5 End-to-end local verification of `infra/localstack/init` + `docker-compose.yml` + Dockerfiles (cross-ref 1.4-1.6).
  **Closed (PR 16, same session) — genuinely run, not assumed, per this task's own explicit instruction. Found and fixed THREE real, previously-undiagnosed local-dev bugs**, none introduced by this session's Terraform work (which only adds `infra/terraform/`, untouched by local dev) but all blocking `docker compose up` from ever actually working end-to-end — a gap the PR 14/15 sessions' own closure notes hint at ("LocalStack container failed to start... a pre-existing local-environment issue, not a code regression") without ever root-causing it. **(1) `localstack/localstack:latest` now requires a paid license.** `docker-compose.yml` pinned an unpinned `:latest` tag, which currently resolves to LocalStack 2026.7.0 — booting it produced `License activation failed... No credentials were found`, exit code 55, even though every service this project requests (`secretsmanager,s3,sns,logs,cloudwatch`) is Community-tier per design.md §22.2's own table. Root-caused by testing `localstack/localstack:3.8` directly (`docker run`) — boots clean, `"edition": "community"`, serves every service this project needs. `docker-compose.yml` now pins `image: localstack/localstack:3.8` with the finding documented inline. **(2) CRLF-corrupted init scripts on Windows.** With this machine's `core.autocrlf=true`, `infra/localstack/init/*.sh` checked out with CRLF line endings; since `docker-compose.yml` bind-mounts that directory straight into the container (no build-context normalization), the raw CRLF bytes reached `bash` and broke `set -euo pipefail` parsing (`invalid option name`) / the `#!/usr/bin/env bash` shebang itself (`env: 'bash\r': No such file or directory`) — all 4 init scripts silently failed (LocalStack's own script runner logged `exit code 127`, easy to miss, no obvious link back to line endings). Fixed by stripping `\r` from all 4 scripts (`tr -d '\r'`) and adding a new `.gitattributes` (`*.sh text eol=lf`, `*.sql text eol=lf`) so this cannot regress for the next Windows clone/checkout. Verified: restarting `localstack` re-ran all 4 scripts successfully (`awslocal secretsmanager list-secrets`/`s3 ls`/`sns list-topics`/`logs describe-log-groups` all show the expected `kureha/dev/kek`/`kureha-spa-dev`/`kureha-audit-alerts-dev`/`/kureha/api/dev` resources). **(3) `.env.local`/`.env.local.example` used `localhost` for a value the containerized `api` service itself needs to resolve.** `AWS_ENDPOINT_URL=http://localhost:4566` and `DATABASE_URL=...@localhost:5432/...` are only correct if the backend runs NATIVELY on the host (hitting the sibling containers' PUBLISHED ports) — `docker-compose.yml`'s own `api` service runs the SAME backend INSIDE a third container, where `localhost` resolves to itself, not its sibling `postgres`/`localstack` containers. This is design.md §22.4's own literal snippet's bug too (not edited there — a locked planning artifact; flagging here rather than retroactively rewriting it). Confirmed by actually building+running `docker compose up`: the `api` container crash-looped (`OSError: Multiple exceptions: [Errno 111] Connect call failed ('127.0.0.1', 5432)`) until `AWS_ENDPOINT_URL`/`DATABASE_URL` were changed to the Docker Compose SERVICE names (`localstack`/`postgres`, resolved by Docker's internal DNS). Also discovered `RUNTIME_DATABASE_URL` was entirely MISSING from the template — `app/db.py` constructs `runtime_engine` unconditionally at import time from `Settings.runtime_database_url`, which silently fell back to its own `localhost`-default, same bug a second time; added it explicitly. Fixed the checked-in `.env.local.example` (with the full "why" documented inline, including "if you run the backend natively instead, override these back to `localhost`") and this machine's own gitignored `.env.local`. **End-to-end result, all verified this session:** `docker compose up -d --build` → all 3 containers healthy/running; Postgres has both `app_user`/`app_runtime` roles and `pgcrypto`/`btree_gist` extensions (confirmed via `psql \du`/`\dx`, `infra/postgres/init/{01_extensions,02_app_runtime_role}.sql` still work unmodified); LocalStack has all 4 Community resources; `GET /openapi.json` on the containerized API returns HTTP 200 (full `create_app()` lifespan, including `bootstrap_rbac_catalog_and_grants()`, completes successfully). Stack torn down cleanly (`docker compose down`, no `-v`, volumes preserved) at the end of verification. **Not verified this session:** a full `uv run pytest`/`npm run test` pass inside this exact stack (out of 16.5's literal "does the stack itself come up" scope, and both suites are routinely run against the native/host Postgres port per every prior phase's own convention, not the containerized `api` service) — `npm run build` (frontend static export) WAS run and its real output shape is what 16.4's CloudFront Function rewrite was validated against.

## Phase 17: Cross-Module Integration Tests + Docs/Cleanup

- [ ] 17.1 Full RLS sweep: cross-tenant and cross-site/role access returns zero rows on every tenant-scoped table. Ref §14.
- [ ] 17.2 End-to-end: schedule via chat → confirmation_gate → rbac_gate → persist_and_audit → calendar_sync → audit_logs chain intact.
- [ ] 17.3 HITL + confirmation composition: confirmation-first-then-HITL ordering, checkpoint cleanup after decline, resume after `interrupt()`.
- [ ] 17.4 Session/security: refresh reuse-detection, live active-status kill-on-next-request, thread_id ownership rejection.
- [ ] 17.5 Update `backend/README.md`/`frontend/README.md` with run instructions; remove scaffolding placeholders (`app/main.py` hello-world, default Next.js page).
