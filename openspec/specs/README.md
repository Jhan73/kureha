# Specs fuente de verdad — Kureha

Vacío por ahora. Se llena por dominio (`specs/{domain}/spec.md`) cuando
`sdd-archive` fusiona los delta-specs de un cambio completado y verificado.
No editar a mano mientras un cambio está activo en `changes/`.
