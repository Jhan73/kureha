"""Clinical scope module: `ClinicalScopePolicy` inbound+outbound classifier
interface (design.md §8.7). The LLM-backed implementation lands in
tasks.md Phase 12 -- this module only defines the contract."""
